apiVersion: v1
kind: Service
metadata:
  name: {{ include "opskg.fullname" . }}
  labels:
    {{- include "opskg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opskg.selectorLabels" . | nindent 4 }}