---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '57b29809-6d6f-42ac-89b9-19e4c8709198'
  PropagateID: '57b29809-6d6f-42ac-89b9-19e4c8709198'
  ReservedCode1: 'c0cd500e-10e2-414c-98bd-807ef1711917'
  ReservedCode2: 'c0cd500e-10e2-414c-98bd-807ef1711917'
---

# 路线图实现偏差核对与下一步演进计划

> 核对日期：2026-07-16（2026-09-03 复审更新）
> 基线：Sprint 6-10+ 路线图 + 项目能力全面审计报告 2026-07

---

## 一、路线图实现核对

### 1.1 Sprint 7：质量基建 + 核心旅程补齐（2 周）

| 任务 | 规划 | 实际 | 偏差 |
|------|------|------|------|
| S7-1 修复 exporter.py Bug | 修复 `re` 未定义 | ✅ 已修复 | 无 |
| S7-2 API 测试 collection 失败 | 添加 conftest.py | ✅ conftest.py 存在，38 测试可收集 | 无 |
| S7-3 main.py 拆分为 APIRouter | 12-15 个子模块 | ✅ 343 行，27 个 include_router | 无 |
| S7-4 ruff 全量修复 | 0 错误 | 🟡 1 个 E902 残留（test_integration.py 文件路径） | 微小偏差 |
| S7-5 Wiki `[[wikilink]]` 渲染 | 可点击跳转 | ✅ WikiView.vue 正确处理 | 无 |
| S7-6 LLM Wiki Q&A 界面 | 搜索框 + LLM 回答 | ✅ WikiQueryView.vue 完整实现 | 无 |
| S7-7 Lint 健康检查仪表盘 | 6 类检测结果 | ✅ WikiHealthView.vue 含全功能 | 无 |
| S7-8 漂移监控界面 | stale 列表 + 重编译 | ✅ 内置于 WikiHealthView | 无 |

**Sprint 7 完成度：95%**（仅 ruff 1 个残留）

### 1.2 Sprint 8：AIOps 前端 + 后端工程化（2 周）

| 任务 | 规划 | 实际 | 偏差 |
|------|------|------|------|
| F6 Runbook 工作台 | POST /runbook/generate | ✅ RunbookView.vue 完整 | 无 |
| F7 Incident 管理 | GET /incidents + 关闭 | ✅ IncidentView.vue 完整 | 无 |
| F8 变更关联 | GET /changes + 关联 | ✅ ChangesView.vue 完整 | 无 |
| F9 服务拓扑 | GET /topology + vue-flow | ✅ TopologyView.vue 完整 | 无 |
| P2-1.1 向量检索 + RRF | 双路召回融合 | ✅ 已交付 | 无 |
| P2-2.2 incident 状态机 | 5 状态序列 | ✅ 已交付 | 无 |
| P2-5.1 MCP 协议升级 | resources/prompts | ✅ 已交付 | 无 |
| P2-4.1 拓扑共现推断 | 上下文共现 | ✅ 已交付 | 无 |
| P2-2.1~2.3 告警/指纹 | 去重 + noise_filtered | ✅ 已交付 | 无 |
| P2-1.2 实体抽取缓存 | rule_extractor | ✅ 已交付 | 无 |
| P2-4.2~4.7 拓扑增强 | 别名/导出/清理/快照/metadata/冗余 | ✅ 全部已交付 | 无 |
| P2-5.2~5.8 MCP 增强 | auth/batch/annotation/SSE/JSON Schema | ✅ 全部已交付 | 无 |

**Sprint 8 完成度：100%**

### 1.3 Sprint 9：增强模块 + 体验优化（2 周）

| 任务 | 规划 | 实际 | 偏差 |
|------|------|------|------|
| F10 模板管理 | CRUD + 渲染 | ✅ TemplatesView.vue | 无 |
| F11 版本控制 | diff + 回滚 | ✅ VersionsView.vue | 无 |
| F12 导出中心 | 4 格式导出 | ✅ ExportView.vue | 无 |
| F13 MCP 工具浏览器 | 工具/资源/Prompt | ✅ McpView.vue | 无 |
| F14 知识图谱可视化 | vue-flow 力导向 | ✅ GraphView.vue | 无 |
| 错误边界 | ErrorBoundary | ✅ 已交付 | 无 |
| 侧栏重组 | 3 组 → 5 组 | ✅ 已交付 | 无 |
| ESLint/Prettier | 代码风格统一 | ✅ 已交付 | 无 |
| naive-ui tree-shaking | 包体积优化 -72% | ✅ 已交付 | 无 |
| Onboarding 引导 | 5 步 tour | ✅ 已交付 | 无 |

**Sprint 9 完成度：100%**

### 1.4 Sprint 10+：企业级就绪（长期）

| 任务 | 规划 | 实际 | 偏差 |
|------|------|------|------|
| SSO 集成 | SAML/OIDC/LDAP | 🟡 OIDC 已交付（Google/GitHub/Keycloak），SAML/LDAP 未启动 | OIDC 完成，SAML/LDAP 待定 |
| 可观测性 | Prometheus + OTel | ✅ 已交付 | 无 |
| HA 高可用 | 健康探针 + 实例标识 | ✅ 已交付 | 无 |
| Webhook | 事件订阅 + 签名 + 重试 | ✅ 已交付 | 无 |
| Vitest 前端测试 | 30+ 用例 | ✅ 已交付（86 用例） | 超预期 |

**Sprint 10+ 完成度：80%**（仅 SAML/LDAP 未完成）

### 1.5 本次新增功能（超出原路线图）

| 新增功能 | 说明 |
|---------|------|
| PipelineView | 独立流水线工作台：上传→编译→结果，三段式纵向流程 |
| PipelineTraceView | 章节级 LLM 处理前后对比，统计卡片 + 折叠对比 |
| 单章节重编译 | POST /llm-wiki/recompile-section，支持 temperature + prompt 自定义 |
| 章节手工编辑保存 | 编译结果直接编辑，自动构建 frontmatter 保存到 VersionControl |
| compile-trace API | GET /llm-wiki/compile-trace/{doc_id}，返回章节级管道追踪 |
| DocumentsView 跳转 | 编译按钮改为跳转到 PipelineView |

---

## 二、项目能力全面审计关键偏差

### 2.1 P0 安全加固（严重偏差，全部未启动）

> **2026-08 核实更新**：以下 6 项均已修复，详见 CHANGELOG v0.1.0 与代码注释（P0-1 ~ P0-6）。

| 差距项 | 风险 | 状态 |
|--------|------|------|
| 弱密码哈希（SHA-256） | 密码可被彩虹表破解 | ✅ 已修复（bcrypt rounds=12，登录时自动迁移旧哈希，P0-1） |
| OIDC 未验签 | 可伪造 SSO 登录 | ✅ 已修复（id_token 签名验证 + nonce 防重放，P0-2） |
| SSRF 风险 | 可攻击内网服务 | ✅ 已修复（URL 白名单 + DNS 二次检查 + 内网 IP 黑名单，P0-3） |
| logout 不失效 token | 登出后 token 仍可用 | ✅ 已修复（revoke_session 服务端删除 session，P0-4） |
| 登录无限流 | 可暴力破解 | ✅ 已修复（登录 5次/分钟 + API 60次/分钟 + 全局限流中间件，P0-5） |
| 内存 state 破坏 HA | 多实例无法共享状态 | ✅ 已修复（OIDC state 持久化 SQLite，10 分钟 TTL，P0-6） |

### 2.2 P1 生产部署就绪（部分偏差）

> 更新日期：2026-08-05（Sprint 12 完成）

| 差距项 | 状态 |
|--------|------|
| K8s 清单重写 | ✅ 已交付（deploy/k8s + deploy/helm Chart，匹配单镜像架构） |
| Dockerfile 非 root 运行 | ✅ 已交付（USER opskg + 非特权端口 8080） |
| 29 个 verify 脚本未进 CI | ✅ 已交付（全部 40 个 verify 脚本纳入 CI） |
| 依赖锁定（pip-compile + hash） | ✅ 已交付（requirements.txt 由 pip-compile 生成） |
| API 版本化（/api/v1 前缀） | ✅ 已交付（/api/v1 主路径 + /api 向后兼容） |
| 存储层迁移评估（Postgres/MySQL） | ✅ 已评估（见 docs/项目审计报告与演进路线.md） |
| 备份热备（checksum 校验） | ✅ 已交付（backup_all_databases + SHA256 manifest + verify/restore 端点） |

### 2.3 P2 Agent Harness 增强（全部未启动）

> 更新 2026-07-16：以下 9 项已全部落地（CHANGELOG v0.1.0 + Sprint 12-17）。

| 差距项 | 状态 |
|--------|------|
| LLM 重试（指数退避） | ✅ 已实施（resilient.py 指数退避 3 次，区分可重试错误） |
| LLM 降级（ollama → vllm） | ✅ 已实施（fallback 后端链） |
| LLM 限流（token bucket） | ✅ 已实施（TokenBucket + 并发限流） |
| 流式取消机制 | ✅ 已实施（CancelToken 传递到 LLM stream） |
| token 用量指标 | ✅ 已实施（prompt_tokens/completion_tokens 上报 Prometheus） |
| MCP 同步/异步桥接修复 | ✅ 已实施（run_in_executor + 工具线程模型） |
| MCP 工具权限控制 | ✅ 已实施（协议层 JSON Schema 校验 + 权限控制） |
| MCP 工具审计日志 | ✅ 已实施（入参 + 耗时 + 出参摘要，protocol.py） |
| Agent Loop 探索 | ✅ 已实施（agent_loop.py 多步推理循环 MVP） |

### 2.4 P3 知识系统增强（部分已实施）

| 差距项 | 状态 | 说明 |
|--------|------|------|
| Wiki 与图谱融合 | ✅ 已实施 | struct_compile 阶段实现 wiki↔graph↔实体↔文档双向关联（2026-08-03 修订） |
| 知识复利回写校验 | ✅ 已实施 | wiki_query.py `_validate_facts` 两层校验（规则+LLM），`_is_duplicate_fact` 去重，`_llm_verify_facts` LLM 自校验（2026-08-03 核实） |
| 增量合并策略升级 | ✅ 已实施 | struct_compile 实现 4 级匹配 + 增量合并到已有 wiki |
| 统一编译器 | ✅ 已评估-不合并 | compiler.py（图谱层：去重/权威评分/入库）与 wiki_compiler.py（wiki 层：LLM 编译页面）职责分明，wiki_compiler 已调用 KnowledgeCompiler，保持分离是合理设计（2026-08-03 评估） |
| Runbook 生成器升级 | ✅ 已实施 | runbook_generator.py `generate_async` 规则召回 + LLM 编译成 wiki 风格，`_build_frontmatter` 遵循 AGENTS.md §三 骨架，路由 `/runbook/generate-llm` 已暴露（2026-08-03 核实） |
| 置信度门控 | ✅ 已实施 | extract 阶段 confidence_auto=0.85/confidence_review=0.60 三级处置（auto/review/discard） |
| 流水线 Lint 步骤 | ✅ 已实施 | 流水线末尾自动调用 lint_all_async 检测矛盾/stale/orphan/missing/deadlink/OKF（2026-08-03） |
| SectionContribution 持久化 | ✅ 已实施 | compile 阶段记录段落→wiki 页面贡献关系，支持反向追溯（2026-08-03） |
| 合并权威性评分 | ✅ 已实施 | struct_compile 合并时按 V2.1 §7.3 Authority 公式评分，新页面权威性显著更高时覆盖（2026-08-03） |
| 结构集成改名 | ✅ 已实施 | struct_compile 步骤前端标签从"结构编译（章节处理）"改为"结构集成"（2026-08-03） |

> **源真值模型修订（2026-08-03）**：V2.1 原设计中"Wiki 不反写图谱"的单向规则已在实际工程中修订为**双向关联模型**。
> 解析后的 Markdown 文档作为原文档基准，wiki、graph、实体之间建立双向链接与关联。
> struct_compile 阶段的 `_update_graph_relations()` 直接写图谱是正确行为，不再视为偏差。

### 2.5 P4 工程质量（部分偏差）

> 更新 2026-08-05（Sprint 15 完成）

| 差距项 | 状态 |
|--------|------|
| 前端 DRY（formatDate 统一） | ✅ 已完成（utils/format.ts，61 处引用） |
| 前端类型收窄（no-explicit-any） | ✅ 已完成（API 文件统一 api.get<unknown, T>） |
| SSE 解析工具抽取 | ✅ 已完成（utils/sse.ts 共享） |
| 可访问性补全 | ✅ 已完成（wikilink ARIA + 焦点管理） |
| e2e 深度场景 | ✅ 已完成（smoke_e2e.py 上传→编辑→lint→协作） |
| 文档一致性 | ✅ 已完成（README 测试数字修正，见 2026-09-03 更新） |

---

## 三、下一步演进路线图

### 优先级原则

基于审计报告建议：**P0 安全 > P1 生产 > P2 Agent > P3 知识 > P4 工程**

### 阶段 1：安全加固（Sprint 11，1 周）

> 状态：✅ 全部完成（2026-07-16 CHANGELOG v0.1.0 交付）

| 编号 | 任务 | 说明 | 状态 |
|------|------|------|------|
| SEC-1 | 密码哈希升级为 bcrypt/argon2 | 替换 SHA-256，加盐 | ✅ 已完成（auth/models.py bcrypt rounds=12） |
| SEC-2 | OIDC 验签修复 | 验证 id_token 签名 + nonce 检查 | ✅ 已完成（auth/oidc.py `_verify_nonce`） |
| SEC-3 | SSRF 防护 | URL 白名单 + 内网 IP 黑名单 | ✅ 已完成（core/security/ssrf.py） |
| SEC-4 | token 失效机制 | logout 时加入黑名单/过期标记 | ✅ 已完成（revoke_session） |
| SEC-5 | 登录限流 | 基于 IP 的 token bucket 限流 | ✅ 已完成（middleware/rate_limit.py） |
| SEC-6 | 状态持久化 | 将内存 state 迁移到 SQLite/Redis | ✅ 已完成（oidc.py save_state → SQLite） |

### 阶段 2：生产部署就绪（Sprint 12，1 周）

> 编号：✅ 全部完成（2026-08-05 Sprint 12）

| 编号 | 任务 | 说明 | 状态 |
|------|------|------|------|
| PROD-1 | Dockerfile 非 root | `USER opskg` + 非特权端口 8080 | ✅ 已完成 |
| PROD-2 | API 版本化 | 引入 `/api/v1` 前缀，兼容旧路径 | ✅ 已完成 |
| PROD-3 | 依赖锁定 | pip-compile 生成 requirements.txt + hash | ✅ 已完成 |
| PROD-4 | CI 验证脚本 | 将 29 个 verify 脚本分批纳入 CI | ✅ 已完成（40 个全纳入） |
| PROD-5 | 备份热备 | SQLite `.backup` + checksum 校验 | ✅ 已完成（ha/backup.py） |
| PROD-6 | ruff 残留修复 | 修复 test_integration.py E902 | ✅ 已完成（ruff check 全绿） |

### 阶段 3：Agent Harness 增强（Sprint 13，2 周）

> 编号：✅ 全部完成（2026-07-16 CHANGELOG v0.1.0 交付）

| 编号 | 任务 | 说明 | 状态 |
|------|------|------|------|
| AGENT-1 | LLM 调用弹性 | 重试（指数退避 3 次）+ 降级（ollama→vllm） | ✅ 已完成（core/llm/resilient.py） |
| AGENT-2 | LLM 限流 | token bucket 限流器 | ✅ 已完成（TokenBucket） |
| AGENT-3 | 流式取消 | CancellationToken 传递到 LLM stream | ✅ 已完成 |
| AGENT-4 | token 用量指标 | prompt_tokens/completion_tokens 上报 Prometheus | ✅ 已完成 |
| AGENT-5 | MCP 工具审计日志 | 记录工具调用入参/出参/耗时 | ✅ 已完成（mcp/protocol.py） |
| AGENT-6 | Agent Loop MVP | LLM 自主选工具 → 执行 → 观察 → 再选择 | ✅ 已完成（core/llm/agent_loop.py） |

### 阶段 4：知识系统增强（Sprint 14，2 周）

| 编号 | 任务 | 说明 | 状态 |
|------|------|------|------|
| KNOW-1 | Wiki-Graph 融合 | struct_compile 阶段实现 wiki↔graph↔实体↔文档双向关联 | ✅ 已完成 |
| KNOW-2 | 知识复利回写校验 | 回写前 LLM 自校验，避免幻觉污染 | ✅ 已完成 |
| KNOW-3 | 增量合并升级 | struct_compile 实现 4 级匹配 + 增量合并到已有 wiki | ✅ 已完成 |
| KNOW-4 | 统一编译器 | 合并 compiler.py 与 wiki_compiler.py | ✅ 已评估-不合并 |
| KNOW-5 | Runbook 升级 | 规则召回 + LLM 编译成 wiki 风格 Runbook | ✅ 已完成 |
| KNOW-6 | 段落级编译 | compile 阶段从实体遍历改为按段落顺序编译 | ✅ 已完成 |
| KNOW-7 | 源真值模型修订 | 从单向改为双向关联模型，解析后 MD 作为原文档基准 | ✅ 已完成 |
| KNOW-8 | 流水线 Lint 步骤 | 流水线末尾自动调用 lint_all_async 检测 6 类问题 | ✅ 已完成 |
| KNOW-9 | SectionContribution 持久化 | compile 阶段记录段落→wiki 页面贡献关系 | ✅ 已完成 |
| KNOW-10 | 合并权威性评分 | struct_compile 合并时按 V2.1 §7.3 Authority 公式评分 | ✅ 已完成 |

### 阶段 5：工程质量收尾（Sprint 15，1 周）

| 编号 | 任务 | 说明 |
|------|------|------|
| ENG-1 | 前端 DRY | 统一 formatDate/formatFileSize 到 utils/format.ts |
| ENG-2 | 前端类型收窄 | 收窄 `any`，API 文件用 `api.get<unknown, T>` |
| ENG-3 | SSE 工具抽取 | wiki.ts 与 mcp.ts 重复 SSE 解析抽取共享 |
| ENG-4 | 可访问性补全 | wikilink 浮层 ARIA、OnboardingTour 焦点陷阱 |
| ENG-5 | e2e 深度场景 | 上传文档 → 编辑 wiki → 运行 lint → 协作编辑 |
| ENG-6 | 文档一致性 | 修正 verify 数、vitest 数、Python 版本不一致 |

### 阶段 6：知识网络可视化与实时联动（Sprint 16，3 周）

> **规划日期**：2026-08-03
> **背景**：P3 知识系统增强（KNOW-1~10）已全部完成，wiki↔graph↔实体↔文档双向关联模型已落地。
> 本阶段聚焦"让知识网络可见、可感、实时联动"，对齐 AGENTS.md §2.1 双向关联模型中
> "图谱变更触发受影响 Wiki 页面重编译（graph → wiki）"的未实现部分。

**方向1：图谱实时同步**

| 编号 | 任务 | 说明 | 优先级 | 状态 |
|------|------|------|--------|------|
| KNOW-11 | 图谱变更事件总线 | graph_store 写入后发布 `graph_changed` 事件（asyncio.Queue 单机 / Redis pubsub 多机），解耦生产者与消费者 | P0 | ✅ 已完成 |
| KNOW-12 | WebSocket 图谱推送 | 新增 `/realtime/graph` 端点订阅 graph_changed，推送增量变更（新增/合并/删除实体）到前端 GraphView | P0 | ✅ 已完成 |
| KNOW-13 | graph→wiki 自动重编译 | 订阅 graph_event_bus，图谱变更触发受影响 wiki 页面重编译（BFS 影响集合 + 乐观锁 + TTL 防僵尸锁） | P1 | ✅ 已完成 |

**方向2：可视化图谱增强**

| 编号 | 任务 | 说明 | 优先级 | 状态 |
|------|------|------|--------|------|
| KNOW-14 | wiki↔graph 双向关联可视化 | GraphView 点击实体节点 → 侧边栏显示关联 wiki 页面列表（has_entity 关系）；Wiki 页面点击实体 → 跳转图谱高亮 | P0 | ✅ 已完成 |
| KNOW-15 | 章节目录树可视化 | 基于 heading_tree + Wiki 页面挂载，渲染树形目录导航，支持折叠/展开/跳转 | P1 | ✅ 已完成（WikiHeadingTree + /wiki/{slug}/heading-tree，2026-08-04） |
| KNOW-16 | 图谱路径分析 | 新增 `/graph/shortest-path`、`/graph/impact-propagation` API，前端高亮路径，支持影响传播分析 | P2 | ✅ 已完成（EntityPathAnalysis.vue，2026-08-04） |

**方向3：知识网络导航**

| 编号 | 任务 | 说明 | 优先级 | 状态 |
|------|------|------|--------|------|
| KNOW-17 | backlink 关系图 | Wiki 页面侧边栏显示 backlink 关系图（哪些页面引用本页），点击跳转 | P1 | ✅ 已完成（EntityBacklinkGraph.vue，2026-08-04） |
| KNOW-18 | 实体时间演变回放 | 记录实体变更历史（version_control 已有），前端时间轴回放图谱演变 | P2 | ✅ 已完成（EntityHistoryTimeline.vue，2026-08-04） |

**技术要点**：
1. 事件总线选型：单机用 `asyncio.Queue`，多机升级 Redis pubsub（与现有 CollabHub 一致）
2. 增量推送：graph_changed 事件携带 `{action, entity_id, entity_type, changed_fields}`，前端增量更新而非全量刷新
3. graph→wiki 重编译：订阅 graph_event_bus，BFS 沿 backlink 链计算影响集合（深度≤2，上限 20 页），乐观锁防并发重编译，TTL 防僵尸锁；与批量 `detect_graph_drift` 互补最终一致
4. 可视化性能：Vue Flow 已限制 200 节点，路径分析时高亮子图，避免全图重渲染
5. 目录树：复用 heading_tree 骨架 + index.md 分组，前端用 NTree 组件渲染

**KNOW-13 实现说明**（2026-08-03）：
- 新增 `backend/app/realtime/graph_wiki_sync.py`：`GraphWikiSync` 协调器，app lifespan 启动时 `start()`、停止时 `stop()`
- 事件过滤：仅处理实体级变更（upsert/delete/merge），关系级变更交给批量 `detect_graph_drift` 兜底，避免事件风暴
- 影响集合：种子=实体名直接匹配 slug + backlink 来源页；BFS 沿 backlink 扩展（深度≤2），单次上限 20 页防雪崩
- 重编译：从页面 frontmatter `sources` 提取 doc_id，调用 `wiki_compiler.recompile_section`，成功后 `clear_stale`，失败保留 stale 标记
- 乐观锁：`_in_progress` 字典 + 300s TTL，防止并发重编译同一页面与僵尸锁

---

## 四、总体评估

### 路线图完成度

| 阶段 | 完成度 | 备注 |
|------|--------|------|
| Sprint 6（MVP 前端） | 100% | F1-F5 全部交付 |
| Sprint 7（质量基建） | 100% | ruff 残留已修复 |
| Sprint 8（AIOps 前后端） | 100% | 全部交付 |
| Sprint 9（增强 + 体验） | 100% | 全部交付 |
| Sprint 10+（企业级） | 100% | SAML/LDAP 已补齐（S13） |
| **计划内总计** | **~100%** | 原路线图全部完成 |
| 本次新增（流水线） | 100% | PipelineView / TraceView / 章节重编译/编辑 |
| 安全加固（SEC） | 100% | 6 项全部落地 |
| 生产部署（PROD） | 100% | 7 项全部落地 |
| Agent Harness（AGENT） | 100% | 9 项全部落地 |
| 知识可视化（KNOW-11~18） | 100% | 2026-08-04 Sprint 16 完成 |

### 核心结论（2026-09-03 更新）

**原路线图已全部完成（~100%）**。此前文档标注"❌ 待实施"的项（SEC-1~6、PROD-1~6、AGENT-1~6、KNOW-15~18、SAML/LDAP）经代码核实均已在 2026-07~08 交付。当前已无功能性偏差，唯一可继续推进的方向是：新功能规划（如 Multi-Agent 协同、知识图谱深度学习）与长期运维。