# OpsKG 项目全面审计报告与演进路线

> 审计日期：2026-07-06 | 审计范围：后端 + 前端 + 文档 + 产品对标 | 审计视角：项目经理 + 产品经理

---

# 第一部分：项目经理视角审计报告

## 1. 项目概况

| 维度 | 数据 |
|------|------|
| 后端代码 | 10,309 行 Python / 49 个 .py 文件 |
| 前端代码 | 17 个文件 / 5 个页面 / 19 个 API 封装 |
| API 端点 | 80 个（全部集中在 main.py 1658 行） |
| 测试用例 | 23 个可收集（8 通过 / 1 失败 / 38 个 collection 失败） |
| 文档 | 5 份计划文档 + AGENTS.md + README |
| Sprint 完成 | Sprint 1/2/3/5 ✅ + Sprint 6 MVP ✅ / Sprint 4 ⏳ 待启动 |
| 技术栈 | FastAPI + Vue 3 + Naive UI + SQLite + Neo4j + LangGraph |

## 2. 进度审计

### 2.1 Sprint 完成度

| Sprint | 计划内容 | 实际状态 | 偏差 |
|--------|---------|---------|------|
| Sprint 1 | LLM Wiki 范式基建（P0-1~P0-4） | ✅ 完成 | 无偏差 |
| Sprint 2 | 生命周期闭环（P1-1~P1-4） | ✅ 完成 | 无偏差 |
| Sprint 3 | 关键 bug 修复（P2-2.3/P2-3.1） | ✅ 完成 | 无偏差 |
| Sprint 5 | 全流程审计复盘 | ✅ 完成 | 78/78 冒烟测试通过 |
| Sprint 4 | P2 工程化完善 | ❌ 未启动 | **阻断**：30+ 子任务积压 |
| Sprint 6 | 前端 MVP | ✅ 已交付 | 计划标"待启动"但实际已交付（文档未同步） |

**进度健康度：🟡 中等**
- LLM Wiki 核心范式（Sprint 1-3/5）按时交付，质量达标
- Sprint 4（P2 工程化）完全未启动，是最大进度风险
- Sprint 6 前端 MVP 已交付但计划文档未同步更新

### 2.2 里程碑达成率

| 里程碑 | 计划完成时间 | 实际状态 | 达成率 |
|--------|------------|---------|--------|
| P0 生产基线 | Sprint 1-3 | ✅ 全部完成 | 100% |
| P1 竞争力 | Sprint 2-3 | ✅ 基本完成 | 90% |
| P2 AIOps | Sprint 4 | ❌ 未启动 | 0% |
| 前端 MVP | Sprint 6 | ✅ 已交付 | 100%（5/14 模块） |
| 前端完整 | Sprint 6+ | ❌ 未启动 | 36%（5/14） |

## 3. 质量审计

### 3.1 代码质量

| 指标 | 数据 | 评级 |
|------|------|------|
| ruff lint 问题 | 68 个（50 可自动修复） | 🟡 中 |
| 真实 Bug | 1 个（exporter.py `re` 未定义，F821） | 🔴 高 |
| 未使用导入 | 45 个（F401） | 🟡 中 |
| 模糊变量名 | 8 个（E741，使用 `l`） | 🟡 中 |
| main.py 行数 | 1658 行 / 80 端点 | 🔴 需拆分 |
| knowledge/ 模块占比 | 3838 行 / 37% | 🟡 模块过载 |

### 3.2 测试质量

| 指标 | 数据 | 评级 |
|------|------|------|
| API 端点测试覆盖 | 0/80（38 用例 collection 失败） | 🔴 严重 |
| 集成测试通过率 | 8/9 通过（1 个因依赖缺失失败） | 🟡 中 |
| 冒烟测试 | 78 步（smoke_e2e + smoke_llm_wiki） | 🟢 好 |
| 测试文件总行数 | 1578 行 | 🟡 中 |
| pytest 可收集用例 | 23 个 | 🔴 低 |

**测试质量结论：🔴 不达标**
- API 层测试完全失效（`ModuleNotFoundError: app`），80 个端点中 0 个有测试覆盖
- 集成测试仅 9 个用例，覆盖率极低
- 冒烟测试虽全面但为脚本式（非 pytest），不计入 CI 覆盖率

### 3.3 前端质量

| 指标 | 数据 | 评级 |
|------|------|------|
| TypeScript 类型检查 | vue-tsc 0 错误 | 🟢 好 |
| 生产构建 | vite build 通过 | 🟢 好 |
| 包体积 | 1.23 MB（gzip 325 KB） | 🟡 需优化 |
| 未使用依赖 | md-editor-v3 + vue-flow 已装未用 | 🟡 浪费 |
| Wiki Markdown 渲染 | 手写正则，不支持 `[[wikilink]]` | 🔴 核心缺失 |

## 4. 风险审计

### 4.1 风险矩阵

| # | 风险项 | 概率 | 影响 | 风险等级 | 状态 |
|---|--------|------|------|---------|------|
| R1 | API 测试 0 覆盖，回归无保障 | 高 | 高 | 🔴 极高 | 未处理 |
| R2 | exporter.py `re` 未定义，运行时 NameError | 中 | 高 | 🔴 高 | 未处理 |
| R3 | main.py 1658 行单文件，维护困难 | 高 | 中 | 🟡 中 | 未处理 |
| R4 | Sprint 4（P2 工程化）30+ 子任务积压 | 高 | 高 | 🔴 极高 | 未启动 |
| R5 | 前端 9/14 模块未交付 | 高 | 中 | 🟡 中 | 未启动 |
| R6 | 无依赖锁文件（requirements.txt 仅 >= 约束） | 中 | 中 | 🟡 中 | 未处理 |
| R7 | 文档与代码状态不同步 | 中 | 低 | 🟢 低 | 部分处理 |
| R8 | markitdown[docx] 可选依赖缺失 | 低 | 低 | 🟢 低 | 未处理 |

### 4.2 技术债务

| 债务项 | 本金（当前成本） | 利息（增长速率） | 建议还款时机 |
|--------|----------------|----------------|-------------|
| main.py 未拆分 | 每次修改端点需翻 1658 行 | 随端点增加线性增长 | 立即 |
| API 测试缺失 | 无法安全重构 | 随代码增长指数增长 | 立即 |
| ruff 68 问题 | 代码可读性下降 | 稳定（不主动恶化） | Sprint 7 |
| 前端包体积 | 首屏加载 325KB | 稳定 | Sprint 7 |
| 无依赖锁 | 环境不一致风险 | 随依赖增加增长 | Sprint 7 |

## 5. 资源与效率审计

### 5.1 代码效率

| 模块 | 行数 | 端点/函数密度 | 评价 |
|------|------|-------------|------|
| knowledge/ | 3838 | 12 文件 / 16 端点 | 🟡 文件过多，单文件偏小 |
| main.py | 1658 | 80 端点 / 1 文件 | 🔴 过度集中 |
| aiops/ | 1481 | 4 文件 / 16 端点 | 🟢 合理 |
| parsers/ | 660 | 9 文件 / 3 端点 | 🟢 合理 |
| frontend/ | ~1600 | 5 页面 / 19 API | 🟢 合理 |

### 5.2 架构健康度

```
✅ 健康                          ⚠️ 关注                          🔴 不健康
─────────────────────────────────────────────────────────────────────────
LLM Wiki 范式 (95%)              前端完成度 (36%)                 API 测试覆盖 (0%)
LLM 可插拔架构                   main.py 拆分需求                 Sprint 4 积压
AGENTS.md 规范                   依赖管理（无锁）                 exporter.py Bug
配置完整（Docker）               文档同步                         包体积优化
```

## 6. 项目经理结论

### 综合评级：🟡 B-（中等偏下）

**优势**：
- LLM Wiki 核心范式完整落地，四阶段闭环全打通
- 后端 80 个端点功能丰富，P0/P1 基线达标
- 前端 MVP 快速交付，5 模块可用

**劣势**：
- **测试是最大短板**：API 层 0 覆盖，重构无安全网
- **Sprint 4 积压**：30+ P2 子任务未启动，AIOps 能力半成品
- **main.py 技术债**：1658 行单文件已到必须拆分的临界点
- **文档不同步**：计划标"待启动"的工作实际已交付

### 紧急行动项（P0）

1. **修复 exporter.py 的 `re` 未定义 Bug**（1 行修复，阻断运行时）
2. **修复 test_api_endpoints.py 的 collection 失败**（加 conftest.py 或 pytest.ini，恢复 38 个测试）
3. **拆分 main.py**（80 端点 → 按业务域拆分 router 模块）

---

# 第二部分：产品经理视角审计报告

## 1. 产品定位与价值主张

### 1.1 产品定位

> **OpsKG = AI 驱动的运维知识管理与文档生成系统**
> 核心价值：文档 → 知识图谱 → 智能生成 闭环
> 范式对标：Karpathy LLM Wiki（知识编译与结构化沉淀）

### 1.2 差异化竞争力

| 能力 | OpsKG | Confluence | BigPanda | Wiki.js |
|------|-------|-----------|----------|---------|
| LLM 原生知识编译 | ✅ 核心特色 | ❌ | ❌ | ❌ |
| `[[wikilink]]` 双向链接 | ✅ | ❌ | ❌ | ❌ |
| AIOps 事件关联 | 🟡 部分 | ❌ | ✅ 旗舰 | ❌ |
| 运维 Runbook 自动生成 | 🟡 部分 | ❌ | ✅ | ❌ |
| 知识图谱可视化 | ❌ 前端缺失 | ❌ | ✅ | ❌ |

**核心差异化**：LLM 原生 + 知识编译 + 运维特色。但差异化能力的前端展示层缺失。

## 2. 功能完整度审计

### 2.1 后端能力矩阵（80 端点）

| 能力域 | 端点数 | 完成度 | 评价 |
|--------|--------|--------|------|
| 文档管理（上传/解析/CRUD） | 6 | ✅ 100% | 完整 |
| 知识搜索（全文/统计） | 3 | ✅ 100% | 完整 |
| 审查队列（审批/批量） | 5 | ✅ 100% | 完整 |
| Wiki 浏览/编译/Q&A | 16 | ✅ 100% | LLM Wiki 核心闭环 |
| 版本控制（diff/回滚） | 5 | ✅ 100% | 完整 |
| 模板管理（CRUD/渲染） | 6 | ✅ 100% | 完整 |
| 导出（多格式） | 1 | ✅ 100% | 完整 |
| 图谱（上传/查询/可视化） | 6 | ✅ 100% | 后端完整 |
| AIOps 事件关联 | 8 | 🟡 60% | bug 已修，状态机/backlog 待补 |
| AIOps 变更关联 | 4 | 🟡 50% | bug 已修，diff 分析待补 |
| 服务拓扑 | 4 | 🟡 50% | 基础完成，共现推断待补 |
| MCP 协议 | 2 | 🟡 40% | 基础完成，resources/prompts 待补 |
| 认证授权 | 隐含 | ✅ 100% | 3 角色 + token |
| 审计日志 | 隐含 | ✅ 100% | 全请求审计 |

### 2.2 前端能力矩阵（14/14 模块全部交付）

| 模块 | 后端就绪 | 前端交付 | 用户可用 | 差距 |
|------|---------|---------|---------|------|
| F1 仪表盘 | ✅ | ✅ | ✅ | 无 |
| F2 文档管理 | ✅ | ✅ | ✅ | 无 |
| F3 知识搜索 | ✅ | ✅ | ✅ | 无向量搜索 UI |
| F4 审查队列 | ✅ | ✅ | ✅ | 无 |
| F5 Wiki 浏览 | ✅ | ✅ | 🟡 | Markdown 渲染简陋，无 `[[wikilink]]` |
| F6 Runbook 工作台 | ✅ | ✅ | ✅ | 已交付（Sprint 8） |
| F7 Incident 管理 | ✅ | ✅ | ✅ | 已交付（Sprint 8） |
| F8 变更关联 | ✅ | ✅ | ✅ | 已交付（Sprint 8） |
| F9 服务拓扑 | ✅ | ✅ | ✅ | 已交付（Sprint 8） |
| F10 模板管理 | ✅ | ✅ | ✅ | F10 已交付（Sprint 9） |
| F11 版本控制 | ✅ | ✅ | ✅ | F11 已交付（Sprint 9） |
| F12 导出中心 | ✅ | ✅ | ✅ | F12 已交付（Sprint 9） |
| F13 MCP 工具浏览器 | ✅ | ✅ | ✅ | F13 已交付（Sprint 9） |
| F14 知识图谱可视化 | ✅ | ✅ | ✅ | F14 已交付（Sprint 9） |

**前端能力交付率：10/14 = 71%**（F1-F14 全部已交付），后端就绪率 14/14 = 100%。

### 2.3 LLM Wiki 范式完成度

| 范式要素 | 后端实现 | 前端展示 | 用户可感知 |
|---------|---------|---------|-----------|
| L1 Raw（文档持久化） | ✅ | ✅ F2 文档管理 | ✅ |
| L2 Wiki（Markdown + wikilink） | ✅ | 🟡 F5 Wiki 浏览 | 🟡 渲染简陋 |
| L3 Schema（AGENTS.md） | ✅ | ❌ | ❌ 用户无感知 |
| Ingest（编译流程） | ✅ | ❌ | ❌ 无编译入口 |
| Query（Wiki Q&A） | ✅ | ❌ | ❌ 无 Q&A 界面 |
| Lint（健康检查） | ✅ | ❌ | ❌ 无 Lint 报告界面 |
| Maintain（漂移检测） | ✅ | ❌ | ❌ 无漂移监控 |

**范式悖论**：后端 LLM Wiki 闭环达 95%，但用户通过前端几乎无法感知这一核心差异化能力。

## 3. 用户体验审计

### 3.1 用户旅程分析

**核心旅程：上传文档 → 知识编译 → Wiki 浏览 → Q&A 查询**

| 步骤 | 后端能力 | 前端支持 | 体验断点 |
|------|---------|---------|---------|
| 1. 上传文档 | ✅ POST /parsers/parse | ✅ F2 拖拽上传 | 无 |
| 2. 自动编译 Wiki | ✅ POST /llm-wiki/ingest | ❌ 无编译入口 | **用户无法触发编译** |
| 3. 浏览 Wiki 页面 | ✅ GET /llm-wiki/page | ✅ F5 Wiki 浏览 | 🟡 Markdown 渲染简陋 |
| 4. 双向链接导航 | ✅ GET /backlinks | 🟡 手动点击 | **`[[wikilink]]` 未渲染为链接** |
| 5. Wiki Q&A | ✅ POST /llm-wiki/query | ❌ 无 Q&A 界面 | **核心功能不可达** |
| 6. 健康检查 | ✅ POST /llm-wiki/lint | ❌ 无 Lint 界面 | **用户无法看到知识库健康状态** |
| 7. 漂移监控 | ✅ GET /llm-wiki/stale | ❌ 无监控界面 | **用户无法感知知识过时** |

**结论**：核心旅程 7 步中有 4 步在前端断裂，用户无法完整体验 LLM Wiki 价值。

### 3.2 信息架构评估

当前侧栏分组：
```
📊 仪表盘
📄 知识管理
   ├── 文档管理
   ├── 知识搜索
   └── Wiki 浏览
✅ 故障响应
   └── 审查队列
```

**问题**：
1. "故障响应"下只有审查队列，Incident/变更/拓扑等 AIOps 模块缺失
2. LLM Wiki 的核心能力（Q&A/Lint/漂移）在信息架构中无入口
3. 缺少"系统治理"分组（配置/审计/导出/MCP）

### 3.3 竞品体验差距

| 体验维度 | OpsKG | Confluence | Wiki.js | 差距 |
|---------|-------|-----------|---------|------|
| 首页仪表盘 | ✅ 4 卡片 | ✅ 丰富 | ✅ 丰富 | 🟡 卡片信息量少 |
| 文档编辑 | ❌ 无前端编辑 | ✅ 所见即所得 | ✅ Markdown 编辑 | 🔴 大 |
| 搜索体验 | ✅ 基础搜索 | ✅ 混合检索 | ✅ 混合检索 | 🟡 中 |
| 版本 diff | ❌ 无前端 | ✅ 可视化 diff | ✅ 可视化 diff | 🔴 大 |
| 知识图谱 | ❌ 无前端 | ❌ | ❌ | 🟢 差异化机会 |
| 移动端适配 | ❌ 未考虑 | ✅ 响应式 | ✅ 响应式 | 🟡 中 |

## 4. 产品对标差距（更新版）

### 4.1 原始差距追踪

| 差距项 | 原等级 | 当前状态 | 变化 |
|--------|--------|---------|------|
| 文档持久化 | 🔴 严重 | ✅ 已解决 | ↑ |
| RBAC 认证 | 🔴 严重 | ✅ 已解决 | ↑ |
| 审计日志 | 🔴 严重 | ✅ 已解决 | ↑ |
| API 测试 | 🔴 严重 | 🟡 部分解决（collection 失败） | ↑ |
| 备份恢复 | 🔴 大 | ✅ 已解决 | ↑ |
| 全文+向量搜索 | 🔴 大 | ✅ 后端已解决 | ↑ |
| 版本控制 | 🔴 大 | ✅ 后端已解决，❌ 前端缺失 | 🟡 |
| Webhook | 🔴 大 | ⚠️ 部分（Alertmanager 接入在 backlog） | 🟡 |
| 导出功能 | 🔴 大 | ✅ 后端已解决，❌ 前端缺失 | 🟡 |
| 模板管理 | 🔴 大 | ✅ 后端已解决，❌ 前端缺失 | 🟡 |
| SSO 集成 | 🔴 大 | ❌ 未启动 | — |

**🔴 严重差距已解决：9/11 = 82%**，但其中 4 项"后端已解决但前端缺失"，用户不可感知。

### 4.2 新发现的差距

| 差距项 | 等级 | 说明 |
|--------|------|------|
| Wiki 编辑能力缺失 | 🔴 严重 | AGENTS.md 定义了 Ingest/Maintain 工作流，但前端无编辑入口 |
| `[[wikilink]]` 渲染缺失 | 🔴 严重 | 范式核心结构化机制在前端不可见 |
| Q&A 界面缺失 | 🔴 严重 | LLM Wiki 的核心 Query 能力用户不可达 |
| Lint 健康检查 UI 缺失 | 🟡 中 | 6 类检测结果用户不可见 |
| 知识图谱可视化缺失 | 🟡 中 | vue-flow 已装未用，差异化能力未展示 |
| 认证流程缺失 | 🟡 中 | 有 token 注入逻辑但无登录页 |

## 5. 产品化完成度

### 5.1 各维度评估

| 维度 | 完成度 | 趋势 | 关键缺口 |
|------|--------|------|---------|
| 核心闭环（解析→抽取→图谱→生成） | 100% | → | 无 |
| LLM Wiki 范式 | 95% | → | 前端展示层缺失 |
| 测试覆盖 | 15% | ↑ | API 层 0 覆盖 |
| 生产部署 | 85% | ↑ | SSO/HA 缺失 |
| 产品竞争力 | 55% | ↑ | 前端仅 36%，核心能力不可达 |
| AIOps 特色 | 40% | ↑ | Sprint 4 未启动 |
| 企业级就绪 | 15% | → | SSO/HA/多租户/可观测性 |
| **综合产品化** | **~60%** | **↑** | **前端 + AIOps 是主要短板** |

### 5.2 产品经理结论

**核心矛盾**：后端能力丰富（80 端点，LLM Wiki 95%），但用户可感知价值低（前端 36%，核心旅程断裂）。

**最紧迫问题**：不是继续堆后端功能，而是**让已有的 LLM Wiki 核心能力通过前端触达用户**。

---

# 第三部分：下一阶段演进路线计划

> 综合项目经理（进度/质量/风险）与产品经理（功能/体验/竞争力）两份审计报告，制定下一阶段演进路线。

## 演进原则

1. **前端优先于后端**：后端 80 端点已就绪，前端仅 36%，先让能力触达用户
2. **核心旅程优先于边缘功能**：先补齐 LLM Wiki 核心旅程的前端断点
3. **质量优先于速度**：修复测试 + 拆分 main.py，建立安全网后再加功能
4. **差异化优先于对标**：LLM Wiki 是核心差异化，优先于 Confluence 对标功能

## Sprint 7：质量基建 + 核心旅程补齐（2 周）

### 目标
修复项目质量短板 + 补齐 LLM Wiki 核心旅程的前端断点

### S7-1 修复 exporter.py Bug（P0，0.5 天）
- 修复 `backend/app/export/exporter.py` 的 `re` 未定义
- 验收：export 端点不再 NameError

### S7-2 修复 API 测试 collection 失败（P0，0.5 天）
- 在 `backend/tests/` 添加 `conftest.py` 注入 sys.path
- 或在 `backend/pytest.ini` 配置 `pythonpath = .`
- 验收：38 个 API 测试可被 pytest 收集

### S7-3 main.py 拆分为 APIRouter 模块（P0，2 天）
- 将 80 端点按业务域拆分为 12-15 个 APIRouter 子模块
- main.py 仅保留 lifespan + middleware + health + include_router
- 验收：80 端点全部保留，pytest 全通过

### S7-4 ruff 全量修复（P1，0.5 天）
- `ruff check --fix` 清理 50 个可自动修复问题
- 手动修复剩余 18 个（E741 变量名 + F841 未使用变量）
- 验收：`ruff check backend/app` 0 错误

### S7-5 Wiki `[[wikilink]]` 渲染（P0，1 天）
- 在 WikiView 中使用 md-editor-v3 替代手写正则
- 渲染 `[[slug]]` 为可点击链接
- 验收：Wiki 页面中的 `[[wikilink]]` 可点击跳转

### S7-6 LLM Wiki Q&A 界面（P0，2 天）
- 新增 WikiQueryView.vue
- 搜索框 + LLM 回答 + `[[来源]]` 引用
- 调用 POST /llm-wiki/query
- 验收：用户可提问并获得基于 Wiki 的回答

### S7-7 Lint 健康检查仪表盘（P1，1 天）
- 新增 WikiHealthView.vue
- 展示 6 类检测结果（矛盾/stale/orphan/missing/类型缺失/空章节）
- 调用 POST /llm-wiki/lint + GET /llm-wiki/orphans + GET /llm-wiki/stale
- 验收：用户可查看知识库健康状态

### S7-8 漂移监控界面（P1，1 天）
- 在 WikiHealthView 中增加"漂移监控"标签页
- 展示 stale 页面列表 + 重编译触发按钮
- 调用 GET /llm-wiki/stale + POST /llm-wiki/recompile-stale
- 验收：用户可查看并触发 stale 页面重编译

## Sprint 8：AIOps 前端 + 后端工程化（2 周）

### 目标
交付 AIOps 4 模块前端 + 启动 Sprint 4 后端 P2 工程化

### 前端（S8-F6~F9）

| 任务 | 对接 API | 优先级 |
|------|---------|--------|
| F6 Runbook 工作台 | POST /runbook/generate + GET /runbook/preview | P0 |
| F7 Incident 管理 | GET /events/incidents + POST /close + POST /runbook | P0 |
| F8 变更关联 | GET /changes + POST /correlate | P1 |
| F9 服务拓扑 | GET /topology + GET /impact（vue-flow 可视化） | P1 |

### 后端（Sprint 4 启动）

| 任务 | 模块 | 优先级 | 状态 |
|------|------|--------|------|
| P2-1.1 向量检索 + RRF 融合 | wiki_query.py | P0 | ✅ 已完成 (commit 1c66f40) |
| P2-2.2 incident 状态机 | event_correlator.py | P1 | ✅ 已完成 (commit 7605e70) |
| P2-5.1 MCP 协议升级 + resources/prompts | mcp/ | P1 | ✅ 已完成 (commit a5b59e1) |
| P2-4.1 拓扑关系推断（上下文共现） | topology_builder.py | P2 | ✅ 已完成 (commit 90b9ece) |

#### Sprint 8 后端交付总结

四个 P2 阶段核心后端任务全部交付：

1. **P2-1.1 向量检索 + RRF 融合**：`recall_pages` 升级为 async 双路召回（关键词 + 向量），使用 RRF（k=60）融合；wiki embedding 实现版本级内存缓存避免重复调用 LLM API。
2. **P2-2.2 incident 状态机**：定义 5 状态序列 `open → ack → investigating → mitigated → resolved`，允许前向跳跃与 reopen；`closed` 作为 `resolved` 的 legacy 别名；`_persist_incidents` 改为 INSERT-OR-UPDATE 避免覆盖 status。
3. **P2-5.1 MCP 协议升级**：SERVER_VERSION 0.1.0 → 0.2.0；新增 `resources/list`、`resources/read`、`prompts/list`、`prompts/get` 方法；6 个静态/动态 resources + 5 个 prompt 模板；wiki_qa prompt 用 asyncio 事件循环驱动 async recall_pages。
4. **P2-4.1 拓扑共现推断**：`topology_edges` 新增 `inferred`/`confidence` 列；`infer_cooccurrence_edges()` 基于 overlap coefficient 推断缺失边；按类型推断 relation（Service→Host=RUNS_ON, Service→Component=USES, 同类型=DEPENDS_ON）；新增 POST /topology/infer 端点 + infer_topology MCP 工具。

**测试结果**：38 项 API 端点测试全通过；P2-4.1 共现推断在 56 节点对上正确推断 20 条新边、跳过 18 条已存在边，类型推断与方向约定均符合预期。

#### Sprint 8 收尾后 P2 推进（续）

按 `docs/前端选型与P2完善计划.md` 的执行顺序，继续推进剩余 P2 子任务：

| 任务 | 模块 | 状态 |
|------|------|------|
| P2-2.1 告警指纹去重 | event_correlator.py | ✅ (commit 0ae30ac) |
| P2-2.3 修复 noise_filtered | event_correlator.py | ✅ (commit 0ae30ac) |
| P2-1.2 文档实体抽取缓存 | rule_extractor.py | ✅ (commit ec27775) |
| P2-3.1 修复 type_weight | wiki_query.py | ✅ Sprint 3 已修复 (commit c40a26b) |
| P2-4.2 节点别名合并 | topology_builder.py | ✅ (commit f47ff30) |
| P2-4.4 Mermaid/Cytoscape 导出 | topology_builder.py | ✅ (commit 4482a1d) |
| P2-4.5 文档删除时清理拓扑 | topology_builder.py | ✅ (commit 06fbdd5) |
| P2-5.2 + P2-5.6 MCP token auth + batch | mcp_router.py | ✅ (commit bca1cfb) |
| P2-5.7 工具 annotations | mcp/protocol.py | ✅ (commit e0a435b) |
| **P2-4.3 拓扑快照与 diff 检测** | topology_builder.py | ✅ (commit f482ab1) |
| **P2-4.6 节点 metadata 扩展** | topology_builder.py + rule_extractor.py | ✅ (commit 80c150a) |
| **P2-4.7 影响分析结合冗余度** | topology_builder.py + mcp/protocol.py | ✅ (commit 72b8bae) |
| **P2-5.5 SSE 传输 + progress 通知** | mcp/progress.py + mcp_router.py + protocol.py | ✅ (commit 5a71ab3) |
| **P2-5.8 JSON Schema 运行时校验** | mcp/schema_validator.py + protocol.py | ✅ (commit 429a33a) |

**P2-4.3 交付摘要**：

1. `topology_snapshots` 表存储历史快照（label + nodes_json + edges_json + 计数）。
2. `TopologyBuilder` 新增 4 个方法：
   - `save_snapshot(label)` 序列化当前 nodes/edges 存入快照表
   - `list_snapshots(limit)` 按时间倒序列出
   - `get_snapshot(id)` 返回快照详情（含 nodes/edges）
   - `diff_snapshots(a, b)` 对比两个快照，输出 added/removed/changed 的节点与边
3. 节点按 `node_id` 索引、边按 `(source, target, relation)` 索引；changed 比较 `occurrences` / `source_docs` / `inferred` / `confidence`。
4. 新增 4 个 REST 端点：`POST /topology/snapshots`、`GET /topology/snapshots`、`GET /topology/snapshots/{id}`、`POST /topology/diff`。
5. 新增 2 个 MCP 工具：`save_topology_snapshot`（`idempotentHint: false`，每次产生新快照）、`diff_topology_snapshots`（`readOnlyHint: true`，纯对比）。
6. 验证脚本 `scripts/verify_p2_4_3.py` 全部通过：包括无变化 diff、有变化 diff（added/removed/changed 各 1 项）、MCP 工具调用与参数校验、TOOLS/TOOL_HANDLERS/_TOOL_ANNOTATIONS 三表一致性。

**P2-4.6 交付摘要**：

1. `topology_nodes` 表新增 `metadata TEXT DEFAULT '{}'` 列（JSON），字段含 `ip` / `version` / `owner` / `env` / `region` / `capacity` / `replicas` 等节点元数据；旧库通过 `_migrate_node_metadata()` 自动补齐。
2. `TopologyBuilder` 改造：
   - `_merge_to_db()` 持久化 metadata，**旧值优先**合并策略（`merged_meta = {**new_meta, **old_meta}`），多次抽取互补累积
   - `get_topology()` / `get_neighbors()` / `get_node()` 反序列化 metadata 字段
   - 新增 `get_node(node_id)` 方法
   - 新增 `update_node_metadata(node_id, metadata, merge=True)` 方法（手动修正），支持 merge=True（旧字段保留+新字段覆盖同名）和 merge=False（整体替换）两种模式
3. `RuleBasedExtractor` 改造（rule_extractor.py）：
   - 新增 7 个 metadata 抽取正则：`VERSION_RE` / `COMPONENT_VERSION_RE` / `OWNER_RE` / `ENV_RE` / `ENV_INLINE_RE` / `REGION_RE` / `CAPACITY_RE`
   - `_extract_metadata_from_text()` 静态方法：从文本抽取全局 metadata（version/owner/env/region/capacity/replicas）
   - `_env_from_name()` 静态方法：从主机名/服务名推断环境（如 `web-prod-01` → `prod`），并归一化 `production→prod` / `testing→test` / `development→dev`
   - `_extract_from_element()` 中 Host/Service/Component 实体附加 global_meta 字段；Component 实体通过 `COMPONENT_VERSION_RE` 抓取版本号
4. 新增 2 个 REST 端点：
   - `GET /topology/node/{node_id}` 节点详情（含 metadata）
   - `PATCH /topology/node/{node_id}/metadata` 手动更新 metadata（带 verify_token 鉴权）
5. 验证脚本 `scripts/verify_p2_4_6.py` 全部通过：5 项测试覆盖抽取器 metadata 字段、拓扑持久化、增量合并（旧值优先）、手动 merge/replace、router 端点定义。

**P2-4.7 交付摘要**：

1. `impact_analysis(node_name)` 增强（完全向后兼容，原有字段全部保留）：
   - 新增 `redundancy` 字段，含 `node_replicas` / `node_is_spof` / `downstream_impacts`
   - 每个 downstream 产出 `capacity_lost` / `capacity_remaining` / `impact_ratio` / `severity` / `is_spof`
   - `severity` 三档分类：
     - `critical`：SPOF（replicas ≤ 1），下游单点故障，total outage
     - `degraded`：impact_ratio ≥ 0.5（损失一半或以上容量）
     - `minor`：impact_ratio < 0.5（小幅容量损失）
   - `summary` 新增 `critical_count` / `degraded_count` / `minor_count` / `single_points_of_failure` / `blast_radius_score`（impact_ratio 之和，衡量爆炸半径）
   - 本节点若 `replicas ≤ 1` 且有 dependents，标注 `node_is_spof=True`
2. 新增 `_redundancy_analysis(node, downstream)` 静态方法：纯函数，便于单测
3. 修复 `get_neighbors` 返回的 `node` 字段未反序列化 metadata 的问题（改用 `all_nodes[0]`，确保 `node.metadata` 是 dict）
4. MCP 协议 `_tool_impact_analysis` 输出新增 `redundancy` 字段；`impact_analysis` 工具描述更新，说明 P2-4.7 冗余度分析能力
5. 验证脚本 `scripts/verify_p2_4_7.py` 8 项全通过：覆盖 SPOF/degraded/minor 三档、summary 汇总、node_is_spof 标注、MCP 输出 redundancy 字段、向后兼容（原有字段全部保留）

**P2-5.5 交付摘要**：

1. 新增 `app/mcp/progress.py` 进度通知模块：
   - 基于 `contextvars.ContextVar` 的进度回调上下文（async + thread 自动隔离）
   - `emit_progress(message, current, total)` 工具内部调用，无回调时为 no-op
   - `set_progress_callback(cb, token)` / `reset_progress_context()` / `get_progress_token()`
   - 普通 JSON-RPC 路径完全无副作用，仅 SSE 端点设置回调时触发
2. 新增 `POST /mcp/stream` SSE 端点（`StreamingResponse` + `text/event-stream`）：
   - 接收单个 JSON-RPC 2.0 请求，流式推送 `event: progress` + `event: result` + `event: error`
   - 工具执行中通过 `notifications/progress` 推送进度（含 `progressToken` / `progress` / `total` / `message`）
   - 工具在 `ThreadPoolExecutor` 中执行，进度事件通过 `queue.Queue` 传回主循环
   - 批量请求被拒绝（SSE 仅支持单请求），返回 `event: error`
   - 设置 `Cache-Control: no-cache` + `X-Accel-Buffering: no` 防止 nginx 缓冲
3. 工具 instrumentation：`_tool_generate_runbook` 在 5 个阶段推送进度（开始/构造查询/检索完成/生成 MD/完成）；error 路径不推送
4. MCP 协议兼容性：
   - 客户端可在请求 `_meta.progressToken` 中传任意 token（字符串/数值），SSE 通知透传该 token
   - 完全向后兼容：`POST /mcp` 与 `GET /mcp/tools` 端点行为不变
5. 验证脚本 `scripts/verify_p2_5_5.py` 7 项全通过：no-op / callback 触发 / 工具推送 / SSE 流式响应 / progress+result 事件 / 批量拒绝 / progressToken 透传

**P2-5.8 交付摘要**：

1. 新增 `app/mcp/schema_validator.py` 轻量级 JSON Schema 校验器（不引入 `jsonschema` 依赖）：
   - 支持 type: `object` / `string` / `integer` / `number` / `boolean` / `array` / `null`
   - 支持 `required` / `properties` / `enum` / `minimum` / `maximum`
   - 支持 `minLength` / `maxLength` / `items`（数组元素递归校验）
   - `bool` 不被视为 `integer`（符合 JSON Schema 规范）
   - `fill_defaults(args, schema)` 自动填充缺失字段的 `default` 值
2. `protocol.handle_request` 增强：
   - `tools/call` 调用工具前先校验 `arguments` 是否符合工具的 `inputSchema`
   - 校验失败返回 JSON-RPC `-32602 Invalid Params`，错误消息含字段路径（如 `.query: 缺少必填字段`）
   - 校验通过后 `fill_defaults` 填充 default，再交给 handler
   - 新增 `_get_tool_schema(tool_name)` 辅助函数从 `TOOLS` 查询 `inputSchema`
   - 未知工具仍返回 `-32601`（不与参数校验混淆）
3. 向后兼容：
   - 已有合法调用不受影响（`list_incidents` / `list_documents` 等仍正常）
   - handler 内部的 ad-hoc 校验保留（如 `symptom` 为空仍返回 error），优先级在 schema 之后
4. 验证脚本 `scripts/verify_p2_5_8.py` 10 项全通过：基础类型 / required / enum / 数值范围 / default 填充 / `-32602` 缺失+类型+enum 三类错误 / 合法请求 / 未知工具 `-32601`

**F10 模板管理（前端）交付摘要**：

1. 新增 `frontend/src/api/templates.ts` API 层：
   - `Template` / `TemplateListResponse` / `RenderResult` TypeScript 接口
   - `listTemplates(category?)` / `getTemplate(slug)` / `createTemplate(payload)` / `updateTemplate(slug, payload)` / `deleteTemplate(slug)` / `renderTemplate(slug, variables)` 6 个函数对应后端 6 个端点
   - 复用全局 axios 实例的 token 注入拦截器
2. 新增 `frontend/src/views/TemplatesView.vue` 视图（按 TopologyView 卡片列表 + NDrawer 详情 + NModal 编辑/渲染 模式）：
   - 列表：分类筛选 + 卡片展示（slug / 名称 / 分类标签 / 内置/自定义标签 / 描述 / 更新时间）+ 4 个操作按钮（查看/渲染/编辑/删除）
   - 详情抽屉：NDrawer 展示完整字段（slug/名称/分类/类型/描述/创建时间/更新时间）+ NCode 高亮显示模板内容（markdown 语法）+ 跳转编辑/渲染
   - 编辑/创建弹窗：NForm 表单（slug 编辑模式禁用 / 名称 / 分类下拉 / 描述 / 内容多行），内置模板 content 禁用编辑并显示警告
   - 渲染弹窗：JSON 变量输入 + 渲染按钮 + NCode 高亮渲染结果，支持 JSON 解析错误提示
   - 未登录态（无 `opskg_token`）禁用编辑/删除/创建按钮并显示 NAlert 提示
   - 内置模板禁用删除按钮，删除前 useDialog 二次确认
3. 路由注册 `router/index.ts`：`{ path: 'templates', name: 'templates', component: () => import('@/views/TemplatesView.vue'), meta: { title: '模板管理' } }`，懒加载
4. 侧边栏 `AppSidebar.vue` 在「系统治理」分组新增「模板管理」菜单项（📋 图标）
5. Bug 修复：模板说明文案中的 `{{variable}}` 被 Vue 编译器误判为插值，改用 `<code v-pre>` 阻止编译；移除未使用的 `columns` computed（视图采用卡片列表，未用 NDataTable）和 `h` / `NFormInput` 导入
6. 验证：`npm run build` 通过（TemplatesView-B0H9FKHe.js 13.22 kB / gzip 4.44 kB），`npm run typecheck` 通过

**F11 版本控制（前端）交付摘要**：

1. 新增 `frontend/src/api/versions.ts` API 层：
   - `VersionMeta` / `VersionDetail` / `VersionListResponse` / `DiffResponse` / `SaveVersionResult` / `WikiDocSummary` TypeScript 接口
   - `listVersions(docKey)` / `getVersion(docKey, version)` / `diffVersions(docKey, v1, v2)` / `saveVersion(docKey, payload)` / `rollbackVersion(docKey, targetVersion)` / `listWikiDocs(limit, offset)` 6 个函数
   - 关键对接点：`POST /save` 的 `title/content/change_summary/author` 走 query 参数（非 body）；diff 接口版本不存在时返回 200 + `{error}`，前端需检查 `error` 字段
2. 新增 `frontend/src/views/VersionsView.vue` 视图：
   - 顶部：doc_key 输入框 + 加载按钮 + Wiki 文档标签云（点击 `wiki:{slug}` 快速填入）
   - 版本列表：卡片式展示（v{version} / 标题 / 作者 / 变更说明 / 创建时间 / checksum），按时间倒序
   - 操作按钮：预览（弹窗显示 content）、详情（NDrawer 展示完整字段 + 内容 + 回滚）、对比最新（一键对比当前版本与最新版本）、回滚到此版本（NPopconfirm 二次确认，提示"将创建新版本不删除历史"）
   - Diff 弹窗：双 NSelect 选择 v1/v2 + 生成 Diff + 显示 added/removed 行数统计 + NCode 高亮 unified diff 文本
   - 未登录态隐藏回滚按钮
3. 路由注册 `router/index.ts`：`{ path: 'versions', name: 'versions', ... }`，懒加载
4. 侧边栏 `AppSidebar.vue` 在「系统治理」分组新增「版本控制」菜单项（🕒 图标）
5. 验证：`npm run build` 通过（VersionsView-MfZ1KVuS.js），`npm run typecheck` 通过

**F14 知识图谱可视化（前端）交付摘要**：

1. 依赖修正：项目原预装的 `vue-flow@0.3.0` 实际是状态管理库（描述 "To make the state of application managed and processed as data flow"），**不是**图谱可视化库。卸载并改用真正的 `@vue-flow/core@1.x` + `@vue-flow/background` + `@vue-flow/controls` + `@vue-flow/minimap`（基于 d3-zoom 的高交互图谱渲染库）
2. 新增 `frontend/src/api/graph.ts` API 层：
   - `GraphNode` / `GraphLink` / `GraphData` / `GraphStats` / `GraphSearchResult` / `GraphEntityDetail` TypeScript 接口
   - `getGraphVisualize(entityType?, limit=200)` / `getGraphStats()` / `searchGraph(q, limit)` / `getGraphEntity(name)` / `getGraphByType(entityType, limit)` 5 个函数对应后端 5 个端点
   - 关键对接点：返回字段叫 `links` 而非 `edges`（D3.js force 格式）；Neo4j 未连接时返回 `{nodes:[], links:[], error, hint}` 需前端降级处理
3. 新增 `frontend/src/views/GraphView.vue` 视图：
   - 顶部统计卡片：实体总数 / 关系总数 / 当前节点 / 当前边
   - 工具栏：实体类型筛选下拉（11 类）+ 搜索框（点击结果跳转详情）+ 刷新 / 适配视图按钮
   - VueFlow 主画布：自定义力导向布局（50 次迭代：重力 + 连接吸引 + 节点排斥），Background 网格 + Controls 缩放 + MiniMap 小地图
   - 节点按 11 类类型着色（Host 蓝 / Service 橙 / Component 绿 / Parameter 紫 / Command 青 / Procedure 红 / Incident 深红 / Symptom 粉 / Experience 橙黄 / Concept 灰 / Document 紫红）
   - 关系按 11 类着色，RELATED_TO / DEPENDS_ON 边动画
   - 节点点击打开 NDrawer 实体详情：属性表 + 1 跳邻居列表（每条邻居可继续点击下钻，递归探索）
   - Neo4j 不可用时显示 NAlert 警告提示
   - 右上角图例：11 类节点 + 11 类关系颜色对照
4. 路由注册 `router/index.ts`：`{ path: 'graph', name: 'graph', ... }`，懒加载
5. 侧边栏 `AppSidebar.vue` 在「知识管理」分组新增「知识图谱」菜单项（🕸️ 图标）
6. 验证：`npm run build` 通过（GraphView-DFEr6kok.js 221.66 kB / gzip 73.01 kB，含 vue-flow 库），`npm run typecheck` 通过

**F12 导出中心（前端）交付摘要**：

1. 新增 `frontend/src/api/export.ts`：
   - `exportFormatOptions` 4 种格式（markdown / html / text / pdf）+ 描述
   - `exportDocument(payload)` 用独立 axios 调用（绕过全局拦截器以拿到完整 response），`responseType: 'blob'`，从 `Content-Disposition` 解析 RFC 5987 编码的 filename*，兜底用 title+ext
   - `downloadBlob(blob, filename)` 浏览器触发下载
2. `api/index.ts` 导出 `getApiBaseUrl()` 供 export.ts 复用 baseURL
3. 新增 `frontend/src/views/ExportView.vue`：
   - 预设来源区：Wiki 文档标签云（点击调 `listVersions` + `getVersion` 拉最新版 content）+ 模板标签云（点击调 `getTemplate` 拉 content）
   - 表单：标题输入 + 格式下拉（带描述说明）+ 内容 textarea
   - 一键导出按钮：调 exportDocument → downloadBlob，提示文件名 + 大小（KB）
   - PDF 依赖缺失（500 + wkhtmltopdf 关键词）友好提示
   - 底部 Markdown 预览（NCode 高亮）
4. 路由 `/export` + 侧边栏「系统治理」新增「导出中心」（📤 图标）

**F13 MCP 工具浏览器（前端）交付摘要**：

1. 新增 `frontend/src/api/mcp.ts`：
   - `McpTool` / `McpResource` / `McpPrompt` / `JsonRpcRequest` / `JsonRpcResponse` / `SseEvent` 接口
   - `listMcpTools()` 调用便捷接口 `GET /mcp/tools`
   - `mcpJsonRpc(req)` 通用 JSON-RPC 调用（`POST /mcp`，支持批量）
   - `callTool(name, args)` / `listResources()` / `readResource(uri)` / `listPrompts()` / `getPrompt(name, args)` 5 个封装函数，自动解包数组响应
   - `callToolStream(name, args, onEvent, progressToken?, signal?)` SSE 流式调用：用 `fetch` + `ReadableStream` 解析 `event:` / `data:` 帧，回调 `onEvent({type, data})`
2. 新增 `frontend/src/views/McpView.vue`：三 Tab 双栏布局
   - **工具 Tab**：左侧工具列表（13 个，按注解着色 readOnly/destructive/idempotent）+ 右侧详情（NDDescriptions + inputSchema 折叠 + JSON 参数输入 + 调用按钮）
     - SSE 流式开关：开启后用 `callToolStream`，事件日志实时显示 progress/result/error
     - 双层 JSON 自动解包：`result.content[0].text` → `JSON.parse` 显示
   - **资源 Tab**：左侧 URI 列表（wiki://index / topology://graph / documents://list 等）+ 右侧点击读取内容
   - **Prompt Tab**：左侧 5 个 prompt 模板（summarize_incident / generate_runbook_from_symptom / wiki_qa / root_cause_analysis / review_change_impact）+ 右侧参数表单 + 渲染结果
3. 路由 `/mcp` + 侧边栏「系统治理」新增「MCP 浏览器」（🔌 图标）
4. 验证：`npm run typecheck` 通过，`npm run build` 通过

**Sprint 9 体验优化交付摘要**：

1. **P1 前端错误边界**：
   - 新增 `components/common/ErrorBoundary.vue`：基于 `onErrorCaptured` 生命周期钩子，捕获子组件渲染错误，显示友好回退 UI（错误名/消息/堆栈/触发位置 + 重试/刷新按钮）
   - `App.vue` 用 `<ErrorBoundary>` 包裹 `<router-view>`，页面级错误不再白屏
   - `main.ts` 添加 `app.config.errorHandler` 全局错误处理器 + `window.unhandledrejection` 捕获未处理 Promise rejection

2. **P2 侧栏信息架构重组**（3 组 → 5 组）：
   - 概览：仪表盘
   - 知识管理：文档管理 / 知识搜索 / Wiki 浏览 / Wiki Q&A / 知识图谱
   - 质量治理：健康检查 / 审查队列 / 版本控制
   - AIOps：Incident 管理 / 变更关联 / 服务拓扑 / Runbook 工作台
   - 系统工具：模板管理 / 导出中心 / MCP 浏览器
   - 每组 3-5 项，语义更清晰，符合用户任务场景

3. **P2 ESLint/Prettier**：
   - `.eslintrc.cjs`：eslint:recommended + plugin:vue/vue3-recommended + @typescript-eslint/recommended + prettier
   - `.prettierrc`：无分号 / 单引号 / 100 字符宽 / trailingComma all / LF
   - `package.json` 新增 `lint` / `lint:check` / `format` / `format:check` 4 个脚本
   - 全量 `--fix` 修复 314 → 10 问题（0 errors，10 warnings 仅为 v-html 预期警告）

4. **P2 naive-ui 按需引入**：
   - 移除 `main.ts` 中 `app.use(naive)` 全量注册
   - 所有组件已在 `.vue` 文件中显式 import，Vite 自动 tree-shaking
   - **主包体积：1,101 kB → 309 kB（-72%）**，gzip 293 kB → 86 kB（-71%）
   - naive-ui 组件自动拆分为独立 chunk（DrawerContent / Tag / Tabs / Input / FormItem / Select / DataTable 等）

## Sprint 9：增强模块 + 体验优化（2 周）

### 目标
交付第三批前端增强模块 + 前端体验优化

### 前端（S9-F10~F14）

| 任务 | 对接 API | 优先级 |
|------|---------|--------|
| F10 模板管理 | /templates CRUD + /render | P1 |
| F11 版本控制 | /versions diff + 回滚 | P1 |
| F12 导出中心 | POST /export | P2 |
| F13 MCP 工具浏览器 | GET /mcp/tools + POST /mcp | P2 |
| F14 知识图谱可视化 | /graph/* （vue-flow 力导向图） | P1 |

### 体验优化

| 任务 | 说明 | 优先级 | 状态 |
|------|------|--------|------|
| 前端错误边界 | ErrorBoundary + 全局 errorHandler | P1 | ✅ 已交付 |
| 侧栏信息架构重组 | 按用户任务重组导航分组 | P2 | ✅ 已交付（5 组） |
| ESLint/Prettier | 代码风格统一 + pre-commit 钩子 | P2 | ✅ 已交付 |
| naive-ui 按需引入 | 优化包体积（325KB → <150KB） | P2 | ✅ 已交付（-72%） |
| Onboarding 引导 | 5 步新手引导 tour | P3 | ✅ 已交付 |

## Sprint 10+：企业级就绪（长期）

| 任务 | 说明 | 优先级 | 状态 |
|------|------|--------|------|
| P3-1 SSO 集成 | SAML 2.0 + OIDC + LDAP | P2 | 🔄 OIDC 已交付（Google/GitHub/Keycloak），SAML/LDAP 待接入 |
| P3-3 可观测性 | Prometheus 指标 + 分布式追踪 | P2 | ✅ 已交付（Prometheus 指标） |
| P3-4 HA 高可用 | 无状态后端 + 负载均衡 | P3 | ✅ 已交付（健康探针 + 实例标识） |
| Webhook 完整实现 | 事件订阅 + 签名 + 重试 | P2 | ✅ 已交付 |
| Vitest 前端测试 | 30+ 用例覆盖核心组件 | P2 | ✅ 已交付（72 用例） |

### 可观测性交付摘要（Sprint 10+）

**目标**：为 OpsKG 后端提供标准 Prometheus 指标暴露，支持 Grafana / VictoriaMetrics 等抓取。

**交付内容**：

1. **指标模块** [observability/metrics.py](file:///workspace/backend/app/observability/metrics.py)
   - **HTTP 指标**（中间件自动采集）：
     - `opskg_http_requests_total{method, path_template, status}` Counter
     - `opskg_http_request_duration_seconds{method, path_template}` Histogram（11 个 bucket）
     - `opskg_http_requests_in_progress` Gauge
   - **业务指标**（13 个）：documents_total / wiki_pages_total / incidents_open / incidents_total / webhook_subscriptions / webhook_deliveries_total / llm_calls_total / llm_call_duration / search_queries_total / documents_uploaded_total / wiki_published_total / incidents_created_total / errors_total
   - **path_template 低基数**：用 `request.scope["route"].path` 取模板（如 `/documents/{doc_id}`），避免高基数
   - **/metrics 端点**：标准 Prometheus 文本格式，`Content-Type: text/plain; version=1.0.0`
   - **可禁用**：`OPSKG_METRICS_ENABLED=0` 完全关闭

2. **业务指标采集器** [observability/collector.py](file:///workspace/backend/app/observability/collector.py)
   - 每 30s 从存储层拉取数据更新 Gauge
   - DocumentStore / VersionControl / EventCorrelator / WebhookStore
   - FastAPI lifespan 启停后台 task

3. **业务路径埋点**（5 处）
   - `documents_uploaded_total{format}` — parsers_router 上传
   - `wiki_published_total` — wiki_router 发布
   - `incidents_created_total` — events_router 关联生成
   - `search_queries_total` — search_router 查询
   - `webhook_deliveries_total{status, event_type}` — webhook manager 投递

4. **main.py 集成** [main.py](file:///workspace/backend/app/main.py)
   - `setup_metrics_middleware(app)` 注册中间件 + /metrics 端点 + /metrics/internal 子挂载
   - lifespan 启动 collector_task，停止时 cancel

5. **验证脚本** [verify_observability.py](file:///workspace/scripts/verify_observability.py)
   - 8 个测试组、**18 个验证点全部通过**
   - 含路径模板低基数验证、Counter 累加验证、collector 端到端拉取验证

**关键设计决策**：
- **零侵入**：HTTP 指标由中间件自动采集，业务代码只需在关键路径调用 `record_business_metric`
- **容错**：observability 子系统异常绝不影响业务，所有 record_* 调用 try-except 守护
- **低基数**：用 path_template 而非具体 URL，避免 label 爆炸
- **独立 registry**：使用独立 `CollectorRegistry`，避免全局污染
- **标准兼容**：可直接被 Prometheus / Grafana / VictoriaMetrics / Datadog Agent 抓取

**抓取配置示例**（Prometheus）：
```yaml
scrape_configs:
  - job_name: 'opsgkg-backend'
    scrape_interval: 15s
    static_configs:
      - targets: ['localhost:8000']
    metrics_path: '/metrics'
```

### Webhook 完整实现交付摘要（Sprint 10+）

**目标**：从零构建事件订阅系统，支持外部系统订阅 OpsKG 关键事件并接收签名回调。

**交付内容**：

1. **存储层** [webhook_store.py](file:///workspace/backend/app/storage/webhook_store.py)
   - `webhook_subscriptions` 表：id / url / events(JSON) / secret / active / description / timestamps
   - `webhook_deliveries` 表：id / subscription_id / event_type / payload / status / response_code / attempts / next_retry_at / timestamps
   - SQLite + WAL 模式，与项目其他存储一致
   - 订阅 CRUD + secret 轮换 + 投递记录生命周期管理

2. **Webhook Manager** [webhooks/manager.py](file:///workspace/backend/app/webhooks/manager.py)
   - **事件目录**：12 类事件（document/wiki/event/incident/runbook/review/webhook.test）
   - **事件匹配**：精确 / `*` 通配 / `incident.*` 前缀通配
   - **签名**：HMAC-SHA256，header `X-Webhook-Signature: sha256=<hex>`
   - **投递**：异步 httpx.AsyncClient（timeout=10s），不阻塞主请求
   - **重试**：指数退避 [30s, 120s, 600s]，默认 1+3=4 次尝试
   - **后台 worker**：每 15s 扫描到期重试任务（FastAPI lifespan 启停）

3. **HTTP API** [webhook_router.py](file:///workspace/backend/app/routers/webhook_router.py)
   - `GET /webhooks` 列出订阅（不含 secret）
   - `POST /webhooks` 创建订阅（返回明文 secret，仅此一次）
   - `GET /webhooks/events` 事件类型目录
   - `GET/PUT/DELETE /webhooks/{sub_id}` 订阅详情/更新/删除
   - `POST /webhooks/{sub_id}/rotate-secret` 重置签名密钥
   - `POST /webhooks/{sub_id}/test` 发送 `webhook.test` 测试事件
   - `GET /webhooks/{sub_id}/deliveries` 订阅级投递记录
   - `GET /webhooks/deliveries` 全局投递记录（可按 status 过滤）
   - `POST /webhooks/deliveries/retry` 手动触发到期重试扫描

4. **事件触发点接入**（共 7 个）
   - `document.created` / `document.parsed` — parsers_router 上传与解析完成
   - `document.deleted` — documents_router 删除
   - `wiki.published` / `wiki.deleted` — wiki_router 发布与删除
   - `event.ingested` — events_router 事件接入
   - `incident.created` — events_router 关联生成 incident
   - `incident.status_changed` — events_router 状态机迁移
   - `runbook.generated` — events_router incident → runbook 生成

5. **验证脚本** [verify_webhook.py](file:///workspace/scripts/verify_webhook.py)
   - 9 个测试组、**62 个验证点全部通过**
   - 含端到端 mock HTTP server 测试（成功投递 + 签名验证 + 500 重试场景）
   - 含后台 worker 启停测试
   - 含同步上下文 dispatch 不阻塞测试

**关键设计决策**：
- **fire-and-forget**：`dispatch_event` 用 `asyncio.create_task` 后台投递，业务请求绝不阻塞
- **容错**：webhook 子系统任何异常都被捕获并 log，绝不影响业务流程
- **secret 安全**：仅在创建/重置时返回一次明文，list/get 接口永不返回
- **可观察**：每次投递都持久化状态（pending/retry/success/failed/skipped）+ 响应码 + 响应体（截断 4KB）
- **HTTP headers**：`X-Webhook-Signature` / `X-Webhook-Event` / `X-Webhook-Event-Id` / `X-Webhook-Timestamp`，便于接收方验签与去重

### HA 高可用交付摘要（Sprint 10+ P3-4）

**目标**：为 OpsKG 后端提供 K8s 友好的健康探针 + 实例标识，支持多副本部署下的流量调度与故障自愈。

**交付内容**：

1. **HA 模块** [ha/__init__.py](file:///workspace/backend/app/ha/__init__.py)
   - `get_instance_id()` — 优先级：`OPSKG_INSTANCE_ID` env > `settings.instance_id` > `hostname+pid` 自动生成
   - `get_startup_time()` — 进程启动时间（ISO8601 UTC），用于计算 uptime
   - `check_db_connection(db_path)` — SQLite 只读模式（`mode=ro`，timeout=2s）连接检查，返回 `{ok, path, error, table_count}`
   - `collect_health()` — 收集 6 个 DB（documents/versions/webhooks/events/search/templates）状态 + webhook worker 状态 + uptime，返回 `status: ok|degraded|down`
   - `collect_readiness()` — 严格检查：所有 DB ok + webhook worker running，返回 `ready: bool` + `checks` 明细

2. **配置扩展** [config.py](file:///workspace/backend/app/config.py)
   - `instance_id: str = ""` — 实例 ID（空则自动生成）
   - `deployment_mode: str = "standalone"` — `standalone` | `replicated`（多实例 + 共享存储）

3. **main.py 集成** [main.py](file:///workspace/backend/app/main.py)
   - **lifespan 启动日志**：记录 `instance_id` / `deployment_mode` / `started_at`，便于多实例日志关联
   - **`/health` 增强**：返回 `collect_health()` 完整结构（status / instance_id / deployment_mode / uptime_seconds / started_at / dbs / background_workers），HTTP 200 表示进程存活
   - **`/ready` 新增**：返回 `collect_readiness()`，`ready=False` 时返回 HTTP 503，K8s 自动从 Service endpoints 摘除流量

4. **验证脚本** [verify_ha.py](file:///workspace/scripts/verify_ha.py)
   - 9 个测试组、**32 个验证点全部通过**
   - 含 instance_id 三级优先级验证、DB 不存在/存在双场景、/ready 503 失败场景、lifespan 日志验证

**关键设计决策**：
- **liveness vs readiness 分离**：`/health` 宽松（任一 DB ok 即 degraded 而非 down，进程存活就返回 200）；`/ready` 严格（所有 DB ok + worker running 才 ready），符合 K8s 探针语义
- **向后兼容**：单实例默认行为不变，多实例需 `OPSKG_DEPLOYMENT_MODE=replicated` + 共享存储（NFS 或类似）
- **DB 检查容错**：只读模式 + 2s 超时，单 DB 不可达不影响其他 DB 检查，所有异常被捕获
- **实例标识三级降级**：env > settings > hostname+pid，确保任何环境下都有非空 instance_id

**K8s 部署示例**：
```yaml
livenessProbe:
  httpGet: { path: /health, port: 8000 }
  initialDelaySeconds: 10
  periodSeconds: 30
readinessProbe:
  httpGet: { path: /ready, port: 8000 }
  initialDelaySeconds: 5
  periodSeconds: 10
```

### SSO 认证框架交付摘要（Sprint 10+ P3-1 基础）

**目标**：从单一共享 Token 升级为用户级认证 + RBAC 角色控制，为后续 OIDC/SAML/LDAP 外部身份提供者接入打基础。

**交付内容**：

1. **用户与会话模型** [auth/models.py](file:///workspace/backend/app/auth/models.py)
   - `users` 表：id / username / password_hash / role / display_name / email / active / timestamps
   - `sessions` 表：token_hash（SHA256 哈希存储）/ user_id / created_at / expires_at
   - 3 角色：admin（全部权限）> operator（业务写）> viewer（只读），层级继承
   - 密码：salt + SHA256，时序安全比较（`secrets.compare_digest`）
   - Session：token 仅创建时返回明文，DB 存哈希；TTL 默认 24h；自动过期清理
   - `AuthStore` 类：用户 CRUD + 密码验证 + session 生命周期 + bootstrap admin

2. **认证依赖（双模式兼容）** [auth/token_auth.py](file:///workspace/backend/app/auth/token_auth.py)
   - `verify_token` — 向后兼容：开发模式放行 / legacy 共享 token / 新 session token
   - `get_current_user` — 获取用户对象（legacy/开发模式返回 None）
   - `require_role(min_role)` — 角色守卫依赖工厂，权限不足返回 403
   - **向后兼容**：legacy token 视为 admin；开发模式（无 token 配置）放行

3. **认证 API** [routers/auth_router.py](file:///workspace/backend/app/routers/auth_router.py)
   - `POST /auth/login` — 用户名密码登录，返回 session token + user
   - `POST /auth/logout` — 注销
   - `GET /auth/me` — 当前用户信息
   - `GET /auth/roles` — 角色目录与层级
   - `GET /auth/users` — 用户列表（admin）
   - `POST /auth/users` — 创建用户（admin）
   - `PUT /auth/users/{id}` — 更新用户（admin）
   - `DELETE /auth/users/{id}` — 删除用户（admin）
   - `POST /auth/cleanup` — 清理过期 session（admin）

4. **lifespan 集成** [main.py](file:///workspace/backend/app/main.py)
   - 启动时自动引导默认 admin 用户（`OPSKG_BOOTSTRAP_ADMIN_USER` / `OPSKG_BOOTSTRAP_ADMIN_PASSWORD`，默认 admin/admin）

5. **验证脚本** [verify_auth.py](file:///workspace/scripts/verify_auth.py)
   - 10 个测试组、**59 个验证点全部通过**
   - 含用户 CRUD / 密码哈希 / session 生命周期 / 角色层级 / login 端点 / me 端点 / admin 守卫 / 403 场景 / 向后兼容 / bootstrap

**关键设计决策**：
- **双模式兼容**：旧部署（OPSKG_API_TOKEN）无需改动，legacy token 视为 admin；新部署用 /auth/login 签发 session token
- **角色层级继承**：admin ≥ operator ≥ viewer，`require_role("operator")` 允许 admin 和 operator，拒绝 viewer
- **token 哈希存储**：session token 不明文存 DB，存 SHA256 哈希，防 DB 泄露后 token 被复用
- **bootstrap 引导**：首次启动自动创建 admin/admin，可通过环境变量覆盖
- **开发模式不变**：未配置任何 token 时，所有请求放行（返回 anonymous），与 P0-2 行为一致

**后续待接入**（P3-1 完整 SSO）：
- ✅ OIDC（Google / GitHub / Keycloak）— 已交付，详见下方"OIDC SSO 完整接入交付摘要"
- SAML 2.0 — 增加 SP metadata + ACS 端点
- LDAP — 增加 bind 验证 + 用户搜索
- ✅ 前端登录页 + 用户菜单 — 已交付（用户管理 UI 待补）

### OIDC SSO 完整接入交付摘要（Sprint 10+ P3-1）

**目标**：在已交付的认证框架（用户名密码 + RBAC + session）基础上，集成标准 OIDC / OAuth2 协议，支持 Google / GitHub / Keycloak 等外部身份提供者，实现单点登录。

**交付内容**：

1. **OIDC 客户端模块** [auth/oidc.py](file:///workspace/backend/app/auth/oidc.py)
   - **`OIDCProvider` 类**：封装单个 IdP 配置（name / display_name / client_id / client_secret / discovery_url / scopes），运行时缓存 discovery 结果（authorization_endpoint / token_endpoint / userinfo_endpoint）
   - **`parse_providers(json_str)`**：从环境变量 `OIDC_PROVIDERS`（JSON 数组）解析多 IdP 配置，支持 Google / GitHub / Keycloak 等任意 OIDC 兼容 IdP
   - **PKCE 支持**：`generate_pkce()` 生成 S256 code_verifier + code_challenge，防止授权码拦截攻击
   - **State 一次性存储**：`save_state` / `pop_state` 内存存储（TTL 10 分钟），CSRF 防护 + 一次性消费
   - **OIDC 用户映射**：`oidc_mappings` 表（provider + oidc_sub → user_id），首次登录自动创建本地用户（角色由 `oidc_default_role` 配置），二次登录按 sub 查找
   - **Token 交换**：`exchange_code()` 用 authorization code 换取 access_token + id_token，优先从 userinfo 端点获取用户信息，fallback 解析 id_token claims
   - **JWT claims 解析**：`_decode_id_token_claims()` base64 解码 JWT payload（不验证签名，仅作 userinfo 不可用时的 fallback）

2. **OIDC 路由** [routers/oidc_router.py](file:///workspace/backend/app/routers/oidc_router.py)
   - `GET /auth/oidc/status` — 检查 OIDC 是否启用 + 提供者列表（不含 secret）
   - `GET /auth/oidc/providers` — 列出已配置的 OIDC 提供者
   - `GET /auth/oidc/{provider}?redirect=/dashboard` — 重定向到 IdP 授权页（带 PKCE + state + redirect_uri）
   - `GET /auth/oidc/{provider}/callback?code=...&state=...` — IdP 回调：
     1. 验证 state（一次性消费 + provider 匹配）
     2. 用 code + PKCE verifier 换取 access_token
     3. 获取用户信息（userinfo 或 id_token claims）
     4. `find_or_create_user` 查找/创建本地用户
     5. 签发本地 session token
     6. 302 重定向到前端 `/login/callback?token=...&redirect=...`
   - **错误处理**：所有失败场景（state 无效 / token 交换失败 / 用户禁用等）均重定向到前端并附带 `?error=...&error_description=...`

3. **配置扩展** [config.py](file:///workspace/backend/app/config.py)
   - `oidc_providers: str` — JSON 字符串，多 IdP 配置数组
   - `oidc_default_role: str = "viewer"` — OIDC 用户首次登录的默认角色
   - `oidc_redirect_base_url: str` — 后端公网回调基址（空则从请求推断）
   - `frontend_base_url: str = "http://localhost:5173"` — 前端基址（用于 OIDC 完成后重定向）

4. **前端登录页** [views/LoginView.vue](file:///workspace/frontend/src/views/LoginView.vue)
   - 用户名 + 密码登录表单（POST /auth/login）
   - OIDC 提供者按钮（动态加载，按 `/auth/oidc/status` 返回渲染）
   - 默认 admin 提示 + OPSKG_BOOTSTRAP_ADMIN_USER 环境变量说明
   - 渐变背景 + 居中卡片布局

5. **OIDC 回调页** [views/LoginCallbackView.vue](file:///workspace/frontend/src/views/LoginCallbackView.vue)
   - 从 URL query 提取 token（成功）或 error（失败）
   - 成功：调用 `authStore.handleOIDCCallback(token)` 保存 token + 加载用户 → 跳转 redirect 目标
   - 失败：显示错误信息 + 返回登录按钮
   - 处理中：Spinner 动画

6. **认证状态管理** [stores/auth.ts](file:///workspace/frontend/src/stores/auth.ts)
   - `token`（localStorage 持久化 `opskg_token`）+ `user` + `authRequired`（区分 dev/prod 模式）
   - `isAuthenticated` / `isAdmin` / `isOperator` / `displayName` computed
   - `loginWithPassword` / `handleOIDCCallback` / `fetchMe` / `logout` / `loadOIDCProviders` / `redirectToOIDC`
   - **`fetchMe` 智能模式判定**：200+authenticated=true → 已登录；200+authenticated=false → dev 模式放行；401 → 需登录

7. **认证 API 封装** [api/auth.ts](file:///workspace/frontend/src/api/auth.ts)
   - login / logout / getMe / getRoles / 用户 CRUD（admin）
   - getOIDCStatus / listOIDCProviders / redirectToOIDC（window.location 跳转）

8. **路由守卫** [router/index.ts](file:///workspace/frontend/src/router/index.ts)
   - `/login` + `/login/callback` 标记 `meta.public`，无需认证
   - 首次访问受保护路由时调用 `fetchMe` 判定 `authRequired`：
     - `true` + 未登录 → 跳转 `/login?redirect=...`
     - `false`（dev 模式）→ 放行
     - `null`（后端不可达）→ 放行（避免锁死）

9. **AppLayout 用户菜单** [components/layout/AppLayout.vue](file:///workspace/frontend/src/components/layout/AppLayout.vue)
   - 头像 NDropdown：显示用户名 + 角色 + 注销 + 重新查看引导
   - 注销调用 `authStore.logout()` + 跳转 /login
   - 刷新页面时自动 `fetchMe` 恢复用户状态

10. **验证脚本** [verify_oidc.py](file:///workspace/scripts/verify_oidc.py)
    - 10 个测试组、**77 个验证点全部通过**
    - 覆盖：parse_providers 解析 / OIDCProvider 类 / PKCE S256 / State 一次性消费 / find_or_create_user 首次创建+二次查找+用户禁用 / status+providers 端点 / authorize 重定向（mock discovery）/ callback 端到端（mock IdP）/ id_token claims 解析 / OIDC 关闭时状态

11. **前端 Vitest** [stores/auth.spec.ts](file:///workspace/frontend/src/stores/auth.spec.ts)
    - 19 个用例全部通过
    - 覆盖：初始 state / setToken / loginWithPassword 成功+失败 / handleOIDCCallback / fetchMe 三种场景（200+auth/200+dev/401/网络错误）/ logout 成功+失败 / loadOIDCProviders / reset / computed 属性
    - 全前端测试套件：7 文件 **72 用例**全部通过（原 53 + 新增 19）

**关键设计决策**：
- **PKCE 强制**：所有 OIDC 流程都带 PKCE S256 challenge，即使 client_secret 已配置也启用（defense in depth）
- **State 一次性消费**：防止 CSRF + 重放攻击，10 分钟 TTL
- **用户映射表**：`oidc_mappings`（provider, oidc_sub）→ user_id，支持同一 IdP 多账号 + 同一账号多 IdP
- **首次登录自动创建**：用 email 或 sub 作为 username，随机密码（OIDC 用户不走密码登录），default_role 可配置
- **dev 模式兼容**：未配置 OIDC_PROVIDERS 时 `enabled=false`，登录页不显示 OIDC 按钮；未配置任何认证时后端放行，前端不强制登录
- **错误全重定向**：OIDC 流程中任何错误都重定向到前端 `/login/callback?error=...`，避免用户看到原始 500 错误
- **JWT 不验签**：id_token 仅在 userinfo 不可用时作 fallback 解析 claims，生产环境应优先用 userinfo 端点（已带 access_token 验证）

**OIDC 配置示例**（.env）：
```bash
OIDC_PROVIDERS='[
  {
    "name": "google",
    "display_name": "Google",
    "client_id": "xxx.apps.googleusercontent.com",
    "client_secret": "xxx",
    "discovery_url": "https://accounts.google.com/.well-known/openid-configuration",
    "scopes": ["openid", "email", "profile"]
  },
  {
    "name": "github",
    "display_name": "GitHub",
    "client_id": "xxx",
    "client_secret": "xxx",
    "discovery_url": "https://accounts.github.com/.well-known/openid-configuration"
  }
]'
OIDC_DEFAULT_ROLE=viewer
FRONTEND_BASE_URL=https://wiki.example.com
OIDC_REDIRECT_BASE_URL=https://api.example.com
```

**验证结果**：
- 后端 `verify_oidc.py`：10 组 77 验证点通过
- 后端 `verify_auth.py`：59 验证点通过（向后兼容未破坏）
- 前端 `npm test`：7 文件 72 用例通过
- 前端 `vue-tsc --noEmit`：0 错误
- 前端 `npm run build`：通过

**后续待接入**（P3-1 剩余）：
- SAML 2.0 — 增加 SP metadata + ACS 端点（基于 python3-saml 或 pysaml2 库）
- LDAP — 增加 bind 验证 + 用户搜索（基于 ldap3 库）
- 用户管理 UI — admin 角色可在前端管理用户列表（CRUD）

### Onboarding 引导 tour 交付摘要（Sprint 9 残留补齐）

**目标**：为 OpsKG 新用户首次访问提供 5 步引导 tour，介绍核心范式（文档 → 知识编译 → Wiki → Q&A）与侧栏信息架构，降低上手门槛。

**交付内容**：

1. **Pinia Store + 持久化** [stores/onboarding.ts](file:///workspace/frontend/src/stores/onboarding.ts)
   - 5 步 tour 定义（welcome → sidebar → documents → wiki → search），每步含 id/title/description/route/selector/placement
   - `useStorage`（VueUse）持久化 `tourCompleted` / `tourSkipped` 到 localStorage（key：`opskg:onboarding:completed|skipped`）
   - 状态机：`activeStepIndex`（-1 表示未激活）+ `isActive`/`currentStep`/`isLastStep`/`progress` computed
   - actions：`startTour` / `nextStep`（最后一步触发完成）/ `prevStep`（首步 no-op）/ `completeTour` / `skipTour` / `resetTour` / `autoStartIfNeeded`

2. **Tour 浮层组件** [components/onboarding/OnboardingTour.vue](file:///workspace/frontend/src/components/onboarding/OnboardingTour.vue)
   - **跨页面导航**：步骤含 `route` 时，先 `router.push` → `nextTick` → 300ms 等待视图渲染，再定位 DOM
   - **Spotlight 高亮**：用 CSS `box-shadow: 0 0 0 9999px rgba(0,0,0,0.5)` 实现遮罩 + 元素镂空效果，无需第三方库
   - **浮层卡片**：步骤徽章（N/5）+ 标题 + 描述（white-space: pre-line 保留换行）+ NProgress 进度条 + 上一步/下一步/跳过/完成按钮
   - **边界修正**：浮层位置自动避免溢出视口（top/left 限制在 `[16, viewport - size - 16]`）
   - **响应式**：window resize 监听重新定位 + watch activeStepIndex 重新定位

3. **AppLayout 集成** [components/layout/AppLayout.vue](file:///workspace/frontend/src/components/layout/AppLayout.vue)
   - 挂载 `<OnboardingTour />` 组件
   - `onMounted` 调用 `autoStartIfNeeded()`，首次访问自动启动
   - `defineExpose({ restartTour })` 暴露重启方法，供用户菜单"重新查看引导"调用

4. **Vitest 验证** [stores/onboarding.spec.ts](file:///workspace/frontend/src/stores/onboarding.spec.ts)
   - 17 个用例全部通过
   - 覆盖：TOUR_STEPS 定义、初始 state、startTour/nextStep/prevStep/completeTour/skipTour/resetTour/autoStartIfNeeded 行为、progress/isLastStep/currentStep computed、已完成/已跳过场景不再自动启动
   - 全前端测试套件：6 文件 53 用例全部通过（原 36 + 新增 17）

**关键设计决策**：
- **零依赖**：未引入 shepherd.js / driver.js 等第三方 tour 库，用 Naive UI 组件 + CSS box-shadow + VueUse useStorage 自研，包体积零增长
- **localStorage 持久化**：用户完成或跳过后不再自动启动，避免重复打扰；可通过 `resetTour` 重启
- **跨页面 tour**：步骤可指定 `route`，浮层会先 router.push 再定位元素，覆盖完整用户旅程（welcome → sidebar → documents → wiki → search）
- **容错降级**：找不到 selector 元素时居中显示，不阻塞 tour 流程
- **与 AppLayout 解耦**：tour 组件独立挂载，store 与组件均可单独复用

**验证结果**：
- `npm run test`（vitest）：6 文件 53 用例通过
- `vue-tsc --noEmit`：0 错误
- `npm run build`：通过

## 演进路线总览

```
Sprint 7（2 周）                Sprint 8（2 周）              Sprint 9（2 周）
┌──────────────────────┐    ┌──────────────────────┐    ┌──────────────────────┐
│ 质量基建              │    │ AIOps 前端            │    │ 增强模块              │
│ · exporter.py Bug 修复│    │ · F6 Runbook 工作台  │    │ · F10 模板管理        │
│ · API 测试修复        │    │ · F7 Incident 管理   │    │ · F11 版本控制        │
│ · main.py 拆分        │    │ · F8 变更关联        │    │ · F12 导出中心        │
│ · ruff 全量修复       │    │ · F9 服务拓扑        │    │ · F13 MCP 浏览器      │
│                      │    │                      │    │ · F14 知识图谱        │
│ 核心旅程补齐          │    │ 后端 P2 工程化        │    │                      │
│ · [[wikilink]] 渲染  │    │ · 向量检索 + RRF     │    │ 体验优化              │
│ · Wiki Q&A 界面      │    │ · incident 状态机    │    │ · 错误边界            │
│ · Lint 健康检查       │    │ · MCP 协议升级       │    │ · 包体积优化          │
│ · 漂移监控            │    │ · 拓扑共现推断       │    │ · Onboarding 引导     │
└──────────────────────┘    └──────────────────────┘    └──────────────────────┘
        ↓                           ↓                           ↓
   前端 36% → 50%             前端 50% → 75%              前端 75% → 100%
   测试 15% → 40%            AIOps 40% → 60%             产品化 60% → 80%
```

## 优先级排序依据

| 优先级 | 依据 | 示例 |
|--------|------|------|
| P0（立即） | 阻断核心功能或有运行时 Bug | exporter.py Bug、API 测试修复、main.py 拆分 |
| P0（核心旅程） | LLM Wiki 差异化能力的前端触达 | `[[wikilink]]` 渲染、Q&A 界面 |
| P1（高价值） | 用户可感知的功能交付 | AIOps 4 模块、Lint 健康检查 |
| P2（竞争力） | 对标竞品的补齐 | 版本 diff、模板管理、导出中心 |
| P3（锦上添花） | 非核心路径优化 | Onboarding、包体积、SSO |

---

## 附录：审计数据汇总

### A. 后端数据

| 指标 | 数值 |
|------|------|
| Python 代码行数 | 10,309 |
| .py 文件数 | 49 |
| API 端点数 | 80 |
| ruff lint 问题 | 68（50 可自动修复） |
| 真实 Bug | 1（F821） |
| pytest 可收集用例 | 23 |
| pytest 通过用例 | 8 |
| 冒烟测试步骤 | 78 |
| 数据库文件 | 6 个 SQLite |
| LLM 后端 | 3（ollama/vllm/openai_compat） |

### B. 前端数据

| 指标 | 数值 |
|------|------|
| 页面数 | 5/14 |
| API 封装函数 | 19 |
| TypeScript 接口 | 12 |
| vue-tsc 错误 | 0 |
| vite build | ✅ 通过 |
| 包体积（gzip） | 325 KB |
| 未使用依赖 | 2（md-editor-v3, vue-flow） |

### C. 产品对标

| 指标 | 数值 |
|------|------|
| 🔴 严重差距已解决 | 9/11 (82%) |
| 前端模块交付率 | 5/14 (36%) |
| 后端端点就绪率 | 14/14 (100%) |
| LLM Wiki 范式完成度 | 95% |
| 综合产品化完成度 | ~60% |
