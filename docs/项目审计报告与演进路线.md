# OpsKG 项目全面审计报告与演进路线

> 审计日期：2026-07-06 | 审计范围：后端 + 前端 + 文档 + 产品对标 | 审计视角：项目经理 + 产品经理

---

# 第一部分：项目经理视角审计报告

## 1. 项目概况

| 维度 | 数据 |
|------|------|
| 后端代码 | 10,309 行 Python / 49 个 .py 文件 |
| 前端代码 | 17 个文件 / 5 个页面 / 19 个 API 封装 |
| API 端点 | 80 个（全部集中在 main.py 1658 行） |
| 测试用例 | 23 个可收集（8 通过 / 1 失败 / 38 个 collection 失败） |
| 文档 | 5 份计划文档 + AGENTS.md + README |
| Sprint 完成 | Sprint 1/2/3/5 ✅ + Sprint 6 MVP ✅ / Sprint 4 ⏳ 待启动 |
| 技术栈 | FastAPI + Vue 3 + Naive UI + SQLite + Neo4j + LangGraph |

## 2. 进度审计

### 2.1 Sprint 完成度

| Sprint | 计划内容 | 实际状态 | 偏差 |
|--------|---------|---------|------|
| Sprint 1 | LLM Wiki 范式基建（P0-1~P0-4） | ✅ 完成 | 无偏差 |
| Sprint 2 | 生命周期闭环（P1-1~P1-4） | ✅ 完成 | 无偏差 |
| Sprint 3 | 关键 bug 修复（P2-2.3/P2-3.1） | ✅ 完成 | 无偏差 |
| Sprint 5 | 全流程审计复盘 | ✅ 完成 | 78/78 冒烟测试通过 |
| Sprint 4 | P2 工程化完善 | ❌ 未启动 | **阻断**：30+ 子任务积压 |
| Sprint 6 | 前端 MVP | ✅ 已交付 | 计划标"待启动"但实际已交付（文档未同步） |

**进度健康度：🟡 中等**
- LLM Wiki 核心范式（Sprint 1-3/5）按时交付，质量达标
- Sprint 4（P2 工程化）完全未启动，是最大进度风险
- Sprint 6 前端 MVP 已交付但计划文档未同步更新

### 2.2 里程碑达成率

| 里程碑 | 计划完成时间 | 实际状态 | 达成率 |
|--------|------------|---------|--------|
| P0 生产基线 | Sprint 1-3 | ✅ 全部完成 | 100% |
| P1 竞争力 | Sprint 2-3 | ✅ 基本完成 | 90% |
| P2 AIOps | Sprint 4 | ❌ 未启动 | 0% |
| 前端 MVP | Sprint 6 | ✅ 已交付 | 100%（5/14 模块） |
| 前端完整 | Sprint 6+ | ❌ 未启动 | 36%（5/14） |

## 3. 质量审计

### 3.1 代码质量

| 指标 | 数据 | 评级 |
|------|------|------|
| ruff lint 问题 | 68 个（50 可自动修复） | 🟡 中 |
| 真实 Bug | 1 个（exporter.py `re` 未定义，F821） | 🔴 高 |
| 未使用导入 | 45 个（F401） | 🟡 中 |
| 模糊变量名 | 8 个（E741，使用 `l`） | 🟡 中 |
| main.py 行数 | 1658 行 / 80 端点 | 🔴 需拆分 |
| knowledge/ 模块占比 | 3838 行 / 37% | 🟡 模块过载 |

### 3.2 测试质量

| 指标 | 数据 | 评级 |
|------|------|------|
| API 端点测试覆盖 | 0/80（38 用例 collection 失败） | 🔴 严重 |
| 集成测试通过率 | 8/9 通过（1 个因依赖缺失失败） | 🟡 中 |
| 冒烟测试 | 78 步（smoke_e2e + smoke_llm_wiki） | 🟢 好 |
| 测试文件总行数 | 1578 行 | 🟡 中 |
| pytest 可收集用例 | 23 个 | 🔴 低 |

**测试质量结论：🔴 不达标**
- API 层测试完全失效（`ModuleNotFoundError: app`），80 个端点中 0 个有测试覆盖
- 集成测试仅 9 个用例，覆盖率极低
- 冒烟测试虽全面但为脚本式（非 pytest），不计入 CI 覆盖率

### 3.3 前端质量

| 指标 | 数据 | 评级 |
|------|------|------|
| TypeScript 类型检查 | vue-tsc 0 错误 | 🟢 好 |
| 生产构建 | vite build 通过 | 🟢 好 |
| 包体积 | 1.23 MB（gzip 325 KB） | 🟡 需优化 |
| 未使用依赖 | md-editor-v3 + vue-flow 已装未用 | 🟡 浪费 |
| Wiki Markdown 渲染 | 手写正则，不支持 `[[wikilink]]` | 🔴 核心缺失 |

## 4. 风险审计

### 4.1 风险矩阵

| # | 风险项 | 概率 | 影响 | 风险等级 | 状态 |
|---|--------|------|------|---------|------|
| R1 | API 测试 0 覆盖，回归无保障 | 高 | 高 | 🔴 极高 | 未处理 |
| R2 | exporter.py `re` 未定义，运行时 NameError | 中 | 高 | 🔴 高 | 未处理 |
| R3 | main.py 1658 行单文件，维护困难 | 高 | 中 | 🟡 中 | 未处理 |
| R4 | Sprint 4（P2 工程化）30+ 子任务积压 | 高 | 高 | 🔴 极高 | 未启动 |
| R5 | 前端 9/14 模块未交付 | 高 | 中 | 🟡 中 | 未启动 |
| R6 | 无依赖锁文件（requirements.txt 仅 >= 约束） | 中 | 中 | 🟡 中 | 未处理 |
| R7 | 文档与代码状态不同步 | 中 | 低 | 🟢 低 | 部分处理 |
| R8 | markitdown[docx] 可选依赖缺失 | 低 | 低 | 🟢 低 | 未处理 |

### 4.2 技术债务

| 债务项 | 本金（当前成本） | 利息（增长速率） | 建议还款时机 |
|--------|----------------|----------------|-------------|
| main.py 未拆分 | 每次修改端点需翻 1658 行 | 随端点增加线性增长 | 立即 |
| API 测试缺失 | 无法安全重构 | 随代码增长指数增长 | 立即 |
| ruff 68 问题 | 代码可读性下降 | 稳定（不主动恶化） | Sprint 7 |
| 前端包体积 | 首屏加载 325KB | 稳定 | Sprint 7 |
| 无依赖锁 | 环境不一致风险 | 随依赖增加增长 | Sprint 7 |

## 5. 资源与效率审计

### 5.1 代码效率

| 模块 | 行数 | 端点/函数密度 | 评价 |
|------|------|-------------|------|
| knowledge/ | 3838 | 12 文件 / 16 端点 | 🟡 文件过多，单文件偏小 |
| main.py | 1658 | 80 端点 / 1 文件 | 🔴 过度集中 |
| aiops/ | 1481 | 4 文件 / 16 端点 | 🟢 合理 |
| parsers/ | 660 | 9 文件 / 3 端点 | 🟢 合理 |
| frontend/ | ~1600 | 5 页面 / 19 API | 🟢 合理 |

### 5.2 架构健康度

```
✅ 健康                          ⚠️ 关注                          🔴 不健康
─────────────────────────────────────────────────────────────────────────
LLM Wiki 范式 (95%)              前端完成度 (36%)                 API 测试覆盖 (0%)
LLM 可插拔架构                   main.py 拆分需求                 Sprint 4 积压
AGENTS.md 规范                   依赖管理（无锁）                 exporter.py Bug
配置完整（Docker）               文档同步                         包体积优化
```

## 6. 项目经理结论

### 综合评级：🟡 B-（中等偏下）

**优势**：
- LLM Wiki 核心范式完整落地，四阶段闭环全打通
- 后端 80 个端点功能丰富，P0/P1 基线达标
- 前端 MVP 快速交付，5 模块可用

**劣势**：
- **测试是最大短板**：API 层 0 覆盖，重构无安全网
- **Sprint 4 积压**：30+ P2 子任务未启动，AIOps 能力半成品
- **main.py 技术债**：1658 行单文件已到必须拆分的临界点
- **文档不同步**：计划标"待启动"的工作实际已交付

### 紧急行动项（P0）

1. **修复 exporter.py 的 `re` 未定义 Bug**（1 行修复，阻断运行时）
2. **修复 test_api_endpoints.py 的 collection 失败**（加 conftest.py 或 pytest.ini，恢复 38 个测试）
3. **拆分 main.py**（80 端点 → 按业务域拆分 router 模块）

---

# 第二部分：产品经理视角审计报告

## 1. 产品定位与价值主张

### 1.1 产品定位

> **OpsKG = AI 驱动的运维知识管理与文档生成系统**
> 核心价值：文档 → 知识图谱 → 智能生成 闭环
> 范式对标：Karpathy LLM Wiki（知识编译与结构化沉淀）

### 1.2 差异化竞争力

| 能力 | OpsKG | Confluence | BigPanda | Wiki.js |
|------|-------|-----------|----------|---------|
| LLM 原生知识编译 | ✅ 核心特色 | ❌ | ❌ | ❌ |
| `[[wikilink]]` 双向链接 | ✅ | ❌ | ❌ | ❌ |
| AIOps 事件关联 | 🟡 部分 | ❌ | ✅ 旗舰 | ❌ |
| 运维 Runbook 自动生成 | 🟡 部分 | ❌ | ✅ | ❌ |
| 知识图谱可视化 | ❌ 前端缺失 | ❌ | ✅ | ❌ |

**核心差异化**：LLM 原生 + 知识编译 + 运维特色。但差异化能力的前端展示层缺失。

## 2. 功能完整度审计

### 2.1 后端能力矩阵（80 端点）

| 能力域 | 端点数 | 完成度 | 评价 |
|--------|--------|--------|------|
| 文档管理（上传/解析/CRUD） | 6 | ✅ 100% | 完整 |
| 知识搜索（全文/统计） | 3 | ✅ 100% | 完整 |
| 审查队列（审批/批量） | 5 | ✅ 100% | 完整 |
| Wiki 浏览/编译/Q&A | 16 | ✅ 100% | LLM Wiki 核心闭环 |
| 版本控制（diff/回滚） | 5 | ✅ 100% | 完整 |
| 模板管理（CRUD/渲染） | 6 | ✅ 100% | 完整 |
| 导出（多格式） | 1 | ✅ 100% | 完整 |
| 图谱（上传/查询/可视化） | 6 | ✅ 100% | 后端完整 |
| AIOps 事件关联 | 8 | 🟡 60% | bug 已修，状态机/backlog 待补 |
| AIOps 变更关联 | 4 | 🟡 50% | bug 已修，diff 分析待补 |
| 服务拓扑 | 4 | 🟡 50% | 基础完成，共现推断待补 |
| MCP 协议 | 2 | 🟡 40% | 基础完成，resources/prompts 待补 |
| 认证授权 | 隐含 | ✅ 100% | 3 角色 + token |
| 审计日志 | 隐含 | ✅ 100% | 全请求审计 |

### 2.2 前端能力矩阵（5/14 模块）

| 模块 | 后端就绪 | 前端交付 | 用户可用 | 差距 |
|------|---------|---------|---------|------|
| F1 仪表盘 | ✅ | ✅ | ✅ | 无 |
| F2 文档管理 | ✅ | ✅ | ✅ | 无 |
| F3 知识搜索 | ✅ | ✅ | ✅ | 无向量搜索 UI |
| F4 审查队列 | ✅ | ✅ | ✅ | 无 |
| F5 Wiki 浏览 | ✅ | ✅ | 🟡 | Markdown 渲染简陋，无 `[[wikilink]]` |
| F6 Runbook 工作台 | ✅ | ❌ | ❌ | 前端未开发 |
| F7 Incident 管理 | ✅ | ❌ | ❌ | 前端未开发 |
| F8 变更关联 | ✅ | ❌ | ❌ | 前端未开发 |
| F9 服务拓扑 | ✅ | ❌ | ❌ | 前端未开发（vue-flow 已装未用） |
| F10 模板管理 | ✅ | ❌ | ❌ | 前端未开发 |
| F11 版本控制 | ✅ | ❌ | ❌ | 前端未开发 |
| F12 导出中心 | ✅ | ❌ | ❌ | 前端未开发 |
| F13 MCP 工具浏览器 | ✅ | ❌ | ❌ | 前端未开发 |
| F14 知识图谱可视化 | ✅ | ❌ | ❌ | 前端未开发（vue-flow 已装未用） |

**前端能力交付率：5/14 = 36%**，但后端就绪率 14/14 = 100%。

### 2.3 LLM Wiki 范式完成度

| 范式要素 | 后端实现 | 前端展示 | 用户可感知 |
|---------|---------|---------|-----------|
| L1 Raw（文档持久化） | ✅ | ✅ F2 文档管理 | ✅ |
| L2 Wiki（Markdown + wikilink） | ✅ | 🟡 F5 Wiki 浏览 | 🟡 渲染简陋 |
| L3 Schema（AGENTS.md） | ✅ | ❌ | ❌ 用户无感知 |
| Ingest（编译流程） | ✅ | ❌ | ❌ 无编译入口 |
| Query（Wiki Q&A） | ✅ | ❌ | ❌ 无 Q&A 界面 |
| Lint（健康检查） | ✅ | ❌ | ❌ 无 Lint 报告界面 |
| Maintain（漂移检测） | ✅ | ❌ | ❌ 无漂移监控 |

**范式悖论**：后端 LLM Wiki 闭环达 95%，但用户通过前端几乎无法感知这一核心差异化能力。

## 3. 用户体验审计

### 3.1 用户旅程分析

**核心旅程：上传文档 → 知识编译 → Wiki 浏览 → Q&A 查询**

| 步骤 | 后端能力 | 前端支持 | 体验断点 |
|------|---------|---------|---------|
| 1. 上传文档 | ✅ POST /parsers/parse | ✅ F2 拖拽上传 | 无 |
| 2. 自动编译 Wiki | ✅ POST /llm-wiki/ingest | ❌ 无编译入口 | **用户无法触发编译** |
| 3. 浏览 Wiki 页面 | ✅ GET /llm-wiki/page | ✅ F5 Wiki 浏览 | 🟡 Markdown 渲染简陋 |
| 4. 双向链接导航 | ✅ GET /backlinks | 🟡 手动点击 | **`[[wikilink]]` 未渲染为链接** |
| 5. Wiki Q&A | ✅ POST /llm-wiki/query | ❌ 无 Q&A 界面 | **核心功能不可达** |
| 6. 健康检查 | ✅ POST /llm-wiki/lint | ❌ 无 Lint 界面 | **用户无法看到知识库健康状态** |
| 7. 漂移监控 | ✅ GET /llm-wiki/stale | ❌ 无监控界面 | **用户无法感知知识过时** |

**结论**：核心旅程 7 步中有 4 步在前端断裂，用户无法完整体验 LLM Wiki 价值。

### 3.2 信息架构评估

当前侧栏分组：
```
📊 仪表盘
📄 知识管理
   ├── 文档管理
   ├── 知识搜索
   └── Wiki 浏览
✅ 故障响应
   └── 审查队列
```

**问题**：
1. "故障响应"下只有审查队列，Incident/变更/拓扑等 AIOps 模块缺失
2. LLM Wiki 的核心能力（Q&A/Lint/漂移）在信息架构中无入口
3. 缺少"系统治理"分组（配置/审计/导出/MCP）

### 3.3 竞品体验差距

| 体验维度 | OpsKG | Confluence | Wiki.js | 差距 |
|---------|-------|-----------|---------|------|
| 首页仪表盘 | ✅ 4 卡片 | ✅ 丰富 | ✅ 丰富 | 🟡 卡片信息量少 |
| 文档编辑 | ❌ 无前端编辑 | ✅ 所见即所得 | ✅ Markdown 编辑 | 🔴 大 |
| 搜索体验 | ✅ 基础搜索 | ✅ 混合检索 | ✅ 混合检索 | 🟡 中 |
| 版本 diff | ❌ 无前端 | ✅ 可视化 diff | ✅ 可视化 diff | 🔴 大 |
| 知识图谱 | ❌ 无前端 | ❌ | ❌ | 🟢 差异化机会 |
| 移动端适配 | ❌ 未考虑 | ✅ 响应式 | ✅ 响应式 | 🟡 中 |

## 4. 产品对标差距（更新版）

### 4.1 原始差距追踪

| 差距项 | 原等级 | 当前状态 | 变化 |
|--------|--------|---------|------|
| 文档持久化 | 🔴 严重 | ✅ 已解决 | ↑ |
| RBAC 认证 | 🔴 严重 | ✅ 已解决 | ↑ |
| 审计日志 | 🔴 严重 | ✅ 已解决 | ↑ |
| API 测试 | 🔴 严重 | 🟡 部分解决（collection 失败） | ↑ |
| 备份恢复 | 🔴 大 | ✅ 已解决 | ↑ |
| 全文+向量搜索 | 🔴 大 | ✅ 后端已解决 | ↑ |
| 版本控制 | 🔴 大 | ✅ 后端已解决，❌ 前端缺失 | 🟡 |
| Webhook | 🔴 大 | ⚠️ 部分（Alertmanager 接入在 backlog） | 🟡 |
| 导出功能 | 🔴 大 | ✅ 后端已解决，❌ 前端缺失 | 🟡 |
| 模板管理 | 🔴 大 | ✅ 后端已解决，❌ 前端缺失 | 🟡 |
| SSO 集成 | 🔴 大 | ❌ 未启动 | — |

**🔴 严重差距已解决：9/11 = 82%**，但其中 4 项"后端已解决但前端缺失"，用户不可感知。

### 4.2 新发现的差距

| 差距项 | 等级 | 说明 |
|--------|------|------|
| Wiki 编辑能力缺失 | 🔴 严重 | AGENTS.md 定义了 Ingest/Maintain 工作流，但前端无编辑入口 |
| `[[wikilink]]` 渲染缺失 | 🔴 严重 | 范式核心结构化机制在前端不可见 |
| Q&A 界面缺失 | 🔴 严重 | LLM Wiki 的核心 Query 能力用户不可达 |
| Lint 健康检查 UI 缺失 | 🟡 中 | 6 类检测结果用户不可见 |
| 知识图谱可视化缺失 | 🟡 中 | vue-flow 已装未用，差异化能力未展示 |
| 认证流程缺失 | 🟡 中 | 有 token 注入逻辑但无登录页 |

## 5. 产品化完成度

### 5.1 各维度评估

| 维度 | 完成度 | 趋势 | 关键缺口 |
|------|--------|------|---------|
| 核心闭环（解析→抽取→图谱→生成） | 100% | → | 无 |
| LLM Wiki 范式 | 95% | → | 前端展示层缺失 |
| 测试覆盖 | 15% | ↑ | API 层 0 覆盖 |
| 生产部署 | 85% | ↑ | SSO/HA 缺失 |
| 产品竞争力 | 55% | ↑ | 前端仅 36%，核心能力不可达 |
| AIOps 特色 | 40% | ↑ | Sprint 4 未启动 |
| 企业级就绪 | 15% | → | SSO/HA/多租户/可观测性 |
| **综合产品化** | **~60%** | **↑** | **前端 + AIOps 是主要短板** |

### 5.2 产品经理结论

**核心矛盾**：后端能力丰富（80 端点，LLM Wiki 95%），但用户可感知价值低（前端 36%，核心旅程断裂）。

**最紧迫问题**：不是继续堆后端功能，而是**让已有的 LLM Wiki 核心能力通过前端触达用户**。

---

# 第三部分：下一阶段演进路线计划

> 综合项目经理（进度/质量/风险）与产品经理（功能/体验/竞争力）两份审计报告，制定下一阶段演进路线。

## 演进原则

1. **前端优先于后端**：后端 80 端点已就绪，前端仅 36%，先让能力触达用户
2. **核心旅程优先于边缘功能**：先补齐 LLM Wiki 核心旅程的前端断点
3. **质量优先于速度**：修复测试 + 拆分 main.py，建立安全网后再加功能
4. **差异化优先于对标**：LLM Wiki 是核心差异化，优先于 Confluence 对标功能

## Sprint 7：质量基建 + 核心旅程补齐（2 周）

### 目标
修复项目质量短板 + 补齐 LLM Wiki 核心旅程的前端断点

### S7-1 修复 exporter.py Bug（P0，0.5 天）
- 修复 `backend/app/export/exporter.py` 的 `re` 未定义
- 验收：export 端点不再 NameError

### S7-2 修复 API 测试 collection 失败（P0，0.5 天）
- 在 `backend/tests/` 添加 `conftest.py` 注入 sys.path
- 或在 `backend/pytest.ini` 配置 `pythonpath = .`
- 验收：38 个 API 测试可被 pytest 收集

### S7-3 main.py 拆分为 APIRouter 模块（P0，2 天）
- 将 80 端点按业务域拆分为 12-15 个 APIRouter 子模块
- main.py 仅保留 lifespan + middleware + health + include_router
- 验收：80 端点全部保留，pytest 全通过

### S7-4 ruff 全量修复（P1，0.5 天）
- `ruff check --fix` 清理 50 个可自动修复问题
- 手动修复剩余 18 个（E741 变量名 + F841 未使用变量）
- 验收：`ruff check backend/app` 0 错误

### S7-5 Wiki `[[wikilink]]` 渲染（P0，1 天）
- 在 WikiView 中使用 md-editor-v3 替代手写正则
- 渲染 `[[slug]]` 为可点击链接
- 验收：Wiki 页面中的 `[[wikilink]]` 可点击跳转

### S7-6 LLM Wiki Q&A 界面（P0，2 天）
- 新增 WikiQueryView.vue
- 搜索框 + LLM 回答 + `[[来源]]` 引用
- 调用 POST /llm-wiki/query
- 验收：用户可提问并获得基于 Wiki 的回答

### S7-7 Lint 健康检查仪表盘（P1，1 天）
- 新增 WikiHealthView.vue
- 展示 6 类检测结果（矛盾/stale/orphan/missing/类型缺失/空章节）
- 调用 POST /llm-wiki/lint + GET /llm-wiki/orphans + GET /llm-wiki/stale
- 验收：用户可查看知识库健康状态

### S7-8 漂移监控界面（P1，1 天）
- 在 WikiHealthView 中增加"漂移监控"标签页
- 展示 stale 页面列表 + 重编译触发按钮
- 调用 GET /llm-wiki/stale + POST /llm-wiki/recompile-stale
- 验收：用户可查看并触发 stale 页面重编译

## Sprint 8：AIOps 前端 + 后端工程化（2 周）

### 目标
交付 AIOps 4 模块前端 + 启动 Sprint 4 后端 P2 工程化

### 前端（S8-F6~F9）

| 任务 | 对接 API | 优先级 |
|------|---------|--------|
| F6 Runbook 工作台 | POST /runbook/generate + GET /runbook/preview | P0 |
| F7 Incident 管理 | GET /events/incidents + POST /close + POST /runbook | P0 |
| F8 变更关联 | GET /changes + POST /correlate | P1 |
| F9 服务拓扑 | GET /topology + GET /impact（vue-flow 可视化） | P1 |

### 后端（Sprint 4 启动）

| 任务 | 模块 | 优先级 | 状态 |
|------|------|--------|------|
| P2-1.1 向量检索 + RRF 融合 | wiki_query.py | P0 | ✅ 已完成 (commit 1c66f40) |
| P2-2.2 incident 状态机 | event_correlator.py | P1 | ✅ 已完成 (commit 7605e70) |
| P2-5.1 MCP 协议升级 + resources/prompts | mcp/ | P1 | ✅ 已完成 (commit a5b59e1) |
| P2-4.1 拓扑关系推断（上下文共现） | topology_builder.py | P2 | ✅ 已完成 (commit 90b9ece) |

#### Sprint 8 后端交付总结

四个 P2 阶段核心后端任务全部交付：

1. **P2-1.1 向量检索 + RRF 融合**：`recall_pages` 升级为 async 双路召回（关键词 + 向量），使用 RRF（k=60）融合；wiki embedding 实现版本级内存缓存避免重复调用 LLM API。
2. **P2-2.2 incident 状态机**：定义 5 状态序列 `open → ack → investigating → mitigated → resolved`，允许前向跳跃与 reopen；`closed` 作为 `resolved` 的 legacy 别名；`_persist_incidents` 改为 INSERT-OR-UPDATE 避免覆盖 status。
3. **P2-5.1 MCP 协议升级**：SERVER_VERSION 0.1.0 → 0.2.0；新增 `resources/list`、`resources/read`、`prompts/list`、`prompts/get` 方法；6 个静态/动态 resources + 5 个 prompt 模板；wiki_qa prompt 用 asyncio 事件循环驱动 async recall_pages。
4. **P2-4.1 拓扑共现推断**：`topology_edges` 新增 `inferred`/`confidence` 列；`infer_cooccurrence_edges()` 基于 overlap coefficient 推断缺失边；按类型推断 relation（Service→Host=RUNS_ON, Service→Component=USES, 同类型=DEPENDS_ON）；新增 POST /topology/infer 端点 + infer_topology MCP 工具。

**测试结果**：38 项 API 端点测试全通过；P2-4.1 共现推断在 56 节点对上正确推断 20 条新边、跳过 18 条已存在边，类型推断与方向约定均符合预期。

#### Sprint 8 收尾后 P2 推进（续）

按 `docs/前端选型与P2完善计划.md` 的执行顺序，继续推进剩余 P2 子任务：

| 任务 | 模块 | 状态 |
|------|------|------|
| P2-2.1 告警指纹去重 | event_correlator.py | ✅ (commit 0ae30ac) |
| P2-2.3 修复 noise_filtered | event_correlator.py | ✅ (commit 0ae30ac) |
| P2-1.2 文档实体抽取缓存 | rule_extractor.py | ✅ (commit ec27775) |
| P2-3.1 修复 type_weight | wiki_query.py | ✅ Sprint 3 已修复 (commit c40a26b) |
| P2-4.2 节点别名合并 | topology_builder.py | ✅ (commit f47ff30) |
| P2-4.4 Mermaid/Cytoscape 导出 | topology_builder.py | ✅ (commit 4482a1d) |
| P2-4.5 文档删除时清理拓扑 | topology_builder.py | ✅ (commit 06fbdd5) |
| P2-5.2 + P2-5.6 MCP token auth + batch | mcp_router.py | ✅ (commit bca1cfb) |
| P2-5.7 工具 annotations | mcp/protocol.py | ✅ (commit e0a435b) |
| **P2-4.3 拓扑快照与 diff 检测** | topology_builder.py | ✅ (commit f482ab1) |
| **P2-4.6 节点 metadata 扩展** | topology_builder.py + rule_extractor.py | ✅ (commit 80c150a) |

**P2-4.3 交付摘要**：

1. `topology_snapshots` 表存储历史快照（label + nodes_json + edges_json + 计数）。
2. `TopologyBuilder` 新增 4 个方法：
   - `save_snapshot(label)` 序列化当前 nodes/edges 存入快照表
   - `list_snapshots(limit)` 按时间倒序列出
   - `get_snapshot(id)` 返回快照详情（含 nodes/edges）
   - `diff_snapshots(a, b)` 对比两个快照，输出 added/removed/changed 的节点与边
3. 节点按 `node_id` 索引、边按 `(source, target, relation)` 索引；changed 比较 `occurrences` / `source_docs` / `inferred` / `confidence`。
4. 新增 4 个 REST 端点：`POST /topology/snapshots`、`GET /topology/snapshots`、`GET /topology/snapshots/{id}`、`POST /topology/diff`。
5. 新增 2 个 MCP 工具：`save_topology_snapshot`（`idempotentHint: false`，每次产生新快照）、`diff_topology_snapshots`（`readOnlyHint: true`，纯对比）。
6. 验证脚本 `scripts/verify_p2_4_3.py` 全部通过：包括无变化 diff、有变化 diff（added/removed/changed 各 1 项）、MCP 工具调用与参数校验、TOOLS/TOOL_HANDLERS/_TOOL_ANNOTATIONS 三表一致性。

**P2-4.6 交付摘要**：

1. `topology_nodes` 表新增 `metadata TEXT DEFAULT '{}'` 列（JSON），字段含 `ip` / `version` / `owner` / `env` / `region` / `capacity` / `replicas` 等节点元数据；旧库通过 `_migrate_node_metadata()` 自动补齐。
2. `TopologyBuilder` 改造：
   - `_merge_to_db()` 持久化 metadata，**旧值优先**合并策略（`merged_meta = {**new_meta, **old_meta}`），多次抽取互补累积
   - `get_topology()` / `get_neighbors()` / `get_node()` 反序列化 metadata 字段
   - 新增 `get_node(node_id)` 方法
   - 新增 `update_node_metadata(node_id, metadata, merge=True)` 方法（手动修正），支持 merge=True（旧字段保留+新字段覆盖同名）和 merge=False（整体替换）两种模式
3. `RuleBasedExtractor` 改造（rule_extractor.py）：
   - 新增 7 个 metadata 抽取正则：`VERSION_RE` / `COMPONENT_VERSION_RE` / `OWNER_RE` / `ENV_RE` / `ENV_INLINE_RE` / `REGION_RE` / `CAPACITY_RE`
   - `_extract_metadata_from_text()` 静态方法：从文本抽取全局 metadata（version/owner/env/region/capacity/replicas）
   - `_env_from_name()` 静态方法：从主机名/服务名推断环境（如 `web-prod-01` → `prod`），并归一化 `production→prod` / `testing→test` / `development→dev`
   - `_extract_from_element()` 中 Host/Service/Component 实体附加 global_meta 字段；Component 实体通过 `COMPONENT_VERSION_RE` 抓取版本号
4. 新增 2 个 REST 端点：
   - `GET /topology/node/{node_id}` 节点详情（含 metadata）
   - `PATCH /topology/node/{node_id}/metadata` 手动更新 metadata（带 verify_token 鉴权）
5. 验证脚本 `scripts/verify_p2_4_6.py` 全部通过：5 项测试覆盖抽取器 metadata 字段、拓扑持久化、增量合并（旧值优先）、手动 merge/replace、router 端点定义。

## Sprint 9：增强模块 + 体验优化（2 周）

### 目标
交付第三批前端增强模块 + 前端体验优化

### 前端（S9-F10~F14）

| 任务 | 对接 API | 优先级 |
|------|---------|--------|
| F10 模板管理 | /templates CRUD + /render | P1 |
| F11 版本控制 | /versions diff + 回滚 | P1 |
| F12 导出中心 | POST /export | P2 |
| F13 MCP 工具浏览器 | GET /mcp/tools + POST /mcp | P2 |
| F14 知识图谱可视化 | /graph/* （vue-flow 力导向图） | P1 |

### 体验优化

| 任务 | 说明 | 优先级 |
|------|------|--------|
| 前端错误边界 | ErrorBoundary + 全局 errorHandler | P1 |
| 侧栏信息架构重组 | 按用户任务重组导航分组 | P2 |
| ESLint/Prettier | 代码风格统一 + pre-commit 钩子 | P2 |
| naive-ui 按需引入 | 优化包体积（325KB → <150KB） | P2 |
| Onboarding 引导 | 5 步新手引导 tour | P3 |

## Sprint 10+：企业级就绪（长期）

| 任务 | 说明 | 优先级 |
|------|------|--------|
| P3-1 SSO 集成 | SAML 2.0 + OIDC + LDAP | P2 |
| P3-3 可观测性 | Prometheus 指标 + 分布式追踪 | P2 |
| P3-4 HA 高可用 | 无状态后端 + 负载均衡 | P3 |
| Webhook 完整实现 | 事件订阅 + 签名 + 重试 | P2 |
| Vitest 前端测试 | 30+ 用例覆盖核心组件 | P2 |

## 演进路线总览

```
Sprint 7（2 周）                Sprint 8（2 周）              Sprint 9（2 周）
┌──────────────────────┐    ┌──────────────────────┐    ┌──────────────────────┐
│ 质量基建              │    │ AIOps 前端            │    │ 增强模块              │
│ · exporter.py Bug 修复│    │ · F6 Runbook 工作台  │    │ · F10 模板管理        │
│ · API 测试修复        │    │ · F7 Incident 管理   │    │ · F11 版本控制        │
│ · main.py 拆分        │    │ · F8 变更关联        │    │ · F12 导出中心        │
│ · ruff 全量修复       │    │ · F9 服务拓扑        │    │ · F13 MCP 浏览器      │
│                      │    │                      │    │ · F14 知识图谱        │
│ 核心旅程补齐          │    │ 后端 P2 工程化        │    │                      │
│ · [[wikilink]] 渲染  │    │ · 向量检索 + RRF     │    │ 体验优化              │
│ · Wiki Q&A 界面      │    │ · incident 状态机    │    │ · 错误边界            │
│ · Lint 健康检查       │    │ · MCP 协议升级       │    │ · 包体积优化          │
│ · 漂移监控            │    │ · 拓扑共现推断       │    │ · Onboarding 引导     │
└──────────────────────┘    └──────────────────────┘    └──────────────────────┘
        ↓                           ↓                           ↓
   前端 36% → 50%             前端 50% → 75%              前端 75% → 100%
   测试 15% → 40%            AIOps 40% → 60%             产品化 60% → 80%
```

## 优先级排序依据

| 优先级 | 依据 | 示例 |
|--------|------|------|
| P0（立即） | 阻断核心功能或有运行时 Bug | exporter.py Bug、API 测试修复、main.py 拆分 |
| P0（核心旅程） | LLM Wiki 差异化能力的前端触达 | `[[wikilink]]` 渲染、Q&A 界面 |
| P1（高价值） | 用户可感知的功能交付 | AIOps 4 模块、Lint 健康检查 |
| P2（竞争力） | 对标竞品的补齐 | 版本 diff、模板管理、导出中心 |
| P3（锦上添花） | 非核心路径优化 | Onboarding、包体积、SSO |

---

## 附录：审计数据汇总

### A. 后端数据

| 指标 | 数值 |
|------|------|
| Python 代码行数 | 10,309 |
| .py 文件数 | 49 |
| API 端点数 | 80 |
| ruff lint 问题 | 68（50 可自动修复） |
| 真实 Bug | 1（F821） |
| pytest 可收集用例 | 23 |
| pytest 通过用例 | 8 |
| 冒烟测试步骤 | 78 |
| 数据库文件 | 6 个 SQLite |
| LLM 后端 | 3（ollama/vllm/openai_compat） |

### B. 前端数据

| 指标 | 数值 |
|------|------|
| 页面数 | 5/14 |
| API 封装函数 | 19 |
| TypeScript 接口 | 12 |
| vue-tsc 错误 | 0 |
| vite build | ✅ 通过 |
| 包体积（gzip） | 325 KB |
| 未使用依赖 | 2（md-editor-v3, vue-flow） |

### C. 产品对标

| 指标 | 数值 |
|------|------|
| 🔴 严重差距已解决 | 9/11 (82%) |
| 前端模块交付率 | 5/14 (36%) |
| 后端端点就绪率 | 14/14 (100%) |
| LLM Wiki 范式完成度 | 95% |
| 综合产品化完成度 | ~60% |
